{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q10": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q10` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q11": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q11` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q12": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q12` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q13": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q13` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q14": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q14` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q4` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q5": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q5` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q6": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q6` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q7": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q7` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q8": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q8` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q9": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q9` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}}, "state": "# jev-opt hint search --- state format state-v1-2026-09-22, vocabulary v1-2026-09-22\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nPicking KEEP_DEFAULT everywhere reproduces the baseline exactly. A hint is\nworth choosing only if it is likely to make the WHOLE PROGRAM measurably\nfaster: the measurement is end-to-end wall time over the case set below, the\nnoise floor of this machine is about 1% aggregated, and nothing smaller than\n3% counts as a change. Hints that change the program's output are\nrejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (15 of them)\n\nEach section below is named after its question.\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <hifijson::SliceLexer as hifijson::token::Lex>::seq\n               8 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  13.05% of the program's user cycles, counting the code inlined into it wherever LTO put it (it has no hot symbol of its own)\nsize after LTO 2136 LLVM instructions, 0 loop(s) inside it\nattributes now (none)  (the distinct sets over all of them)\n\nsource:\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/token.rs:103 (lines 63-143, the site's own line marked `>`)\n   63      /// This returns:\n   64      ///\n   65      /// - `Some(None)` if the input is \"null\",\n   66      /// - `Some(Some(b))` if the input is a boolean `b` (\"true\" or \"false\"), else\n   67      /// - `None`.\n   68      fn null_or_bool(&mut self) -> Option<Option<bool>> {\n   69          // we are calling this function without having advanced before\n   70          Some(match self.take_next() {\n   71              Some(b'n') if self.strip_prefix(b\"ull\") => None,\n   72              Some(b't') if self.strip_prefix(b\"rue\") => Some(true),\n   73              Some(b'f') if self.strip_prefix(b\"alse\") => Some(false),\n   74              _ => return None,\n   75          })\n   76      }\n   77  \n   78      /// Take next character, discard it, and return mutable handle to lexer.\n   79      ///\n   80      /// This is useful in particular when parsing negative numbers,\n   81      /// where you want to discard `-` and immediately continue.\n   82      fn discarded(&mut self) -> &mut Self {\n   83          self.take_next();\n   84          self\n   85      }\n   86  \n   87      /// Peek at next character, and discard it if it matches the expected character.\n   88      ///\n   89      /// Returns\n   90      /// `Some(())` if the peeked character matched the expected character, else\n   91      /// `None`.\n   92      ///\n   93      /// If [`bool::ok_or_else`] was stable, we could return a `bool` here.\n   94      fn expect(&mut self, pf: impl FnOnce(&mut Self) -> Option<u8>, expect: u8) -> Option<()> {\n   95          if pf(self) == Some(expect) {\n   96              self.take_next().map(|_| ())\n   97          } else {\n   98              None\n   99          }\n  100      }\n  101  \n  102      /// Execute `f` for every item in the comma-separated sequence until `end`.\n  103 >    fn seq<E: From<Expect>, PF, F>(&mut self, end: u8, mut pf: PF, mut f: F) -> Result<(), E>\n  104      where\n  105          PF: FnMut(&mut Self) -> Option<u8>,\n  106          F: FnMut(u8, &mut Self) -> Result<(), E>,\n  107      {\n  108          let mut next = pf(self).ok_or(Expect::ValueOrEnd)?;\n  109          if next == end {\n  110              self.take_next();\n  111              return Ok(());\n  112          };\n  113  \n  114          loop {\n  115              f(next, self)?;\n  116              next = pf(self).ok_or(Expect::CommaOrEnd)?;\n  117              if next == end {\n  118                  self.take_next();\n  119                  return Ok(());\n  120              } else if next == b',' {\n  121                  self.take_next();\n  122                  next = pf(self).ok_or(Expect::Value)?;\n  123              } else {\n  124                  return Err(Expect::CommaOrEnd)?;\n  125              }\n  126          }\n  127      }\n  128  \n  129      /// Parse once using given function and assure that the function has consumed all tokens.\n  130      fn exactly_one<T, E: From<Expect>, PF, F>(&mut self, mut pf: PF, f: F) -> Result<T, E>\n  131      where\n  132          PF: FnMut(&mut Self) -> Option<u8>,\n  133          F: FnOnce(u8, &mut Self) -> Result<T, E>,\n  134      {\n  135          let next = pf(self).ok_or(Expect::Value)?;\n  136          let v = f(next, self)?;\n  137          match pf(self) {\n  138              None => Ok(v),\n  139              Some(_) => Err(Expect::Eof)?,\n  140          }\n  141      }\n  142  }\n  143  \n\nwhat LLVM said about this region in the baseline build:\n  token.rs:96: '_RNvXs_NtCs6u2SwoadH4g_8hifijson4readINtB6_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB4_4Read9take_nextCsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=210, threshold=45)\n  token.rs:96: loop not vectorized: instruction return type cannot be vectorized\n  token.rs:121: '_RNvXs_NtCs6u2SwoadH4g_8hifijson4readINtB6_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB4_4Read9take_nextCsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=210, threshold=45)\n  token.rs:121: loop not vectorized: instruction return type cannot be vectorized\n  token.rs:83: '_RNvXs_NtCs6u2SwoadH4g_8hifijson4readINtB6_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB4_4Read9take_nextCsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=210, threshold=45)\n  token.rs:141: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtCslKRKzoMO8pS_8jaq_json3ValEBD_' not inlined into '_RNvNtCslKRKzoMO8pS_8jaq_json4read12parse_single' because too costly to inline (cost=515, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\n               5 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  9.07% of the program's user cycles, counting the code inlined into it wherever LTO put it (it has no hot symbol of its own)\nsize after LTO 1984 LLVM instructions, 0 loop(s) inside it\nattributes now (none)  (the distinct sets over all of them)\n\nsource:\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:187 (lines 147-227, the site's own line marked `>`)\n  147      }\n  148  \n  149      /// Ensure that once `process` has returned `true`, the string has actually terminated.\n  150      fn finish(self, mut next: impl FnMut() -> Option<u8>) -> Result<(), Error> {\n  151          match self.error {\n  152              Some(e) => Err(e),\n  153              None if self.escape.is_some() => Err(escape::Error::Eof)?,\n  154              None if next() != Some(b'\"') => Err(Error::Eof),\n  155              None => Ok(()),\n  156          }\n  157      }\n  158  }\n  159  \n  160  /// String lexing that does never allocate.\n  161  pub trait Lex: escape::Lex {\n  162      /// Read a string without saving it.\n  163      fn str_ignore(&mut self) -> Result<(), Error> {\n  164          self.str_foreach(|_| ())\n  165      }\n  166  \n  167      /// Run a function for every character of the string.\n  168      fn str_foreach(&mut self, f: impl FnMut(u8)) -> Result<(), Error> {\n  169          let mut state = State::default();\n  170          self.foreach_until(f, |c| state.process(c));\n  171          state.finish(|| self.take_next())\n  172      }\n  173  }\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187 >    fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194              matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n  215              }\n  216          }\n  217      }\n  218  }\n  219  \n  220  impl<T> LexWrite for T where T: Read + Write {}\n  221  \n  222  /// String lexing that always allocates when lexing from iterators and\n  223  /// allocates when lexing from slices that contain escape sequences.\n  224  pub trait LexAlloc: LexWrite {\n  225      /// The type of string that we are lexing into.\n  226      type Str: AsRef<str>;\n  227  \n\nwhat LLVM said about this region in the baseline build:\n  str.rs:188: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  str.rs:185: eliminating argument (3)\n  str.rs:181: load of type i8 not eliminated\n  str.rs:180: load of type i8 not eliminated\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <hifijson::SliceLexer as hifijson::write::Write>::write_until\n               6 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.75% of the program's user cycles, counting the code inlined into it wherever LTO put it (it has no hot symbol of its own)\nsize after LTO 331 LLVM instructions, 0 loop(s) inside it\nattributes now cold  (the distinct sets over all of them)\n\nsource:\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/write.rs:33 (lines 1-57, the site's own line marked `>`)\n    1  /// Writing input to bytes.\n    2  ///\n    3  /// This is useful to capture a part of the input,\n    4  /// without allocating when the input is a slice.\n    5  pub trait Write {\n    6      /// Type of bytes to write to.\n    7      type Bytes: core::convert::AsRef<[u8]> + Default;\n    8  \n    9      /// Write input to `bytes` until `stop` yields true.\n   10      ///\n   11      /// The `stop` function gets the previously read bytes and the current byte.\n   12      ///\n   13      /// This function does not return a new [`Self::Bytes`] to avoid allocations.\n   14      ///\n   15      /// ~~~\n   16      /// fn test<L: hifijson::Write>(lexer: &mut L) {\n   17      ///     let mut bytes = L::Bytes::default();\n   18      ///     lexer.write_until(&mut bytes, |c| c == b' ');\n   19      ///     assert_eq!(bytes.as_ref(), b\"Hello\");\n   20      ///     lexer.write_until(&mut bytes, |_| false);\n   21      ///     assert_eq!(bytes.as_ref(), b\" World\");\n   22      /// }\n   23      /// let s = b\"Hello World\";\n   24      /// test(&mut hifijson::SliceLexer::new(s));\n   25      /// test(&mut hifijson::IterLexer::new(s.iter().copied().map(Ok::<_, ()>)));\n   26      /// ~~~\n   27      fn write_until(&mut self, bytes: &mut Self::Bytes, stop: impl FnMut(u8) -> bool);\n   28  }\n   29  \n   30  impl<'a> Write for crate::SliceLexer<'a> {\n   31      type Bytes = &'a [u8];\n   32  \n   33 >    fn write_until(&mut self, bytes: &mut Self::Bytes, stop: impl FnMut(u8) -> bool) {\n   34          let pos = self.slice.iter().copied().position(stop);\n   35          let pos = pos.unwrap_or(self.slice.len());\n   36          *bytes = &self.slice[..pos];\n   37          self.slice = &self.slice[pos..]\n   38      }\n   39  }\n   40  \n   41  #[cfg(feature = \"alloc\")]\n   42  impl<E, I: Iterator<Item = Result<u8, E>>> Write for crate::IterLexer<E, I> {\n   43      type Bytes = alloc::vec::Vec<u8>;\n   44  \n   45      fn write_until(&mut self, bytes: &mut Self::Bytes, mut stop: impl FnMut(u8) -> bool) {\n   46          use crate::Read;\n   47          bytes.clear();\n   48          while let Some(c) = self.peek_next() {\n   49              if stop(c) {\n   50                  return;\n   51              } else {\n   52                  self.take_next();\n   53                  bytes.push(c)\n   54              }\n   55          }\n   56      }\n   57  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:34: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:34: load of type ptr not eliminated\n  write.rs:34: load of type i64 not eliminated\n  write.rs:34: load eliminated by PRE\n  write.rs:34: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  write.rs:30: loop not vectorized: call instruction cannot be vectorized\n  write.rs:27: loop not vectorized: call instruction cannot be vectorized\n  write.rs:25: loop not vectorized: call instruction cannot be vectorized\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q4\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q5\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q6\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  2.71% of the program's user cycles\nsize after LTO 515 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  utf8.rs:127: advising against unrolling the loop because it contains a call\n  utf8.rs:130: load of type i8 not eliminated\n  utf8.rs:123: '_RNvNtCs2ubz5g4KsBC_4core9panicking18panic_bounds_check' not inlined into '_RINvNtCs7XvlSH3kU6m_11regex_bites4utf86decodeRShEB4_' because it should never be inlined (cost=never): noinline function attribute\n  utf8.rs:131: '_RNvNtCsifSg31dUhSV_4bstr5ascii20first_non_ascii_byte' not inlined into '_RNvXs_NtCsifSg31dUhSV_4bstr4utf8NtB4_5CharsNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iterator5count' because too costly to inline (cost=365, threshold=45)\n  utf8.rs:131: '_RNvNtCsifSg31dUhSV_4bstr5ascii20first_non_ascii_byte' inlined into '_RNvMNtCslKRKzoMO8pS_8jaq_json4funsNtB4_3Val6length' with (cost=always): benefit over cost at callsite _RNvXs_NtCsifSg31dUhSV_4bstr4utf8NtB4_5CharsNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iterator5count:6:32 @ _RNvMNtCslKRKzoMO8pS_8jaq_json4funsNtB4_3Val6length:4:52;\n  utf8.rs:131: '_RNvNtCsifSg31dUhSV_4bstr5ascii20first_non_ascii_byte' inlined into '_RNvXs1i_NtCs2ubz5g4KsBC_4core3fmtRNtNtCsifSg31dUhSV_4bstr4bstr4BStrNtB6_7Display3fmtCstofKklK7j5_7jaq_all' with (cost=365, threshold=919) at callsite _RNvXs_NtCsifSg31dUhSV_4bstr4utf8NtB4_5CharsNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iterator5count:6:32 @ _RNvXNtNtCsifSg31dUhSV_4bstr5impls4bstrNtNtB6_4bstr4BStrNtNtCs2ubz5g4KsBC_4core3fmt7Display3fmt:29:43 @ _RNvXs1i_NtCs2ubz5g4KsBC_4core3fmtRNtNtCsifSg31dUhSV_4bstr4bstr4BStrNtB6_7Display3fmtCstofKklK7j5_7jaq_all:0:62;\n  utf8.rs:131: '_RNvNtCsifSg31dUhSV_4bstr5ascii20first_non_ascii_byte' not inlined into '_RNvXs0_NtNtNtCs2ubz5g4KsBC_4core4iter8adapters10filter_mapINtB5_9FilterMapINtNtB7_3zip3ZipNtNtCs7XvlSH3kU6m_11regex_bites5bytes17SubCaptureMatchesNtB1s_12CaptureNamesENCNvNtCs8murSGRldJT_7jaq_std5regex5regex0ENtNtNtB9_6traits8iterator8Iterator4nextB2L_' because too costly to inline (cost=365, threshold=45)\n  utf8.rs:131: '_RNvNtCsifSg31dUhSV_4bstr5ascii20first_non_ascii_byte' inlined into '_RNvXNtNtCsifSg31dUhSV_4bstr5impls4bstrNtNtB6_4bstr4BStrNtNtCs2ubz5g4KsBC_4core3fmt7Display3fmt' with (cost=365, threshold=919) at callsite _RNvXs_NtCsifSg31dUhSV_4bstr4utf8NtB4_5CharsNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iterator5count:6:32 @ _RNvXNtNtCsifSg31dUhSV_4bstr5impls4bstrNtNtB6_4bstr4BStrNtNtCs2ubz5g4KsBC_4core3fmt7Display3fmt:29:43;\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q7\n\nsite kind      one marked function (function attribute)\nfunction       <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as core::ops::function::FnOnce<(jaq_core::path::Part<jaq_json::Val>, jaq_json::Val)>>::call_once\n               2 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  2.53% of the program's user cycles\nsize after LTO 1144 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  function.rs:250: '_RNvXsM_NtNtCsk2wPHUzPKJU_5gimli4read4unitINtB5_14AttributeValueINtNtB7_12endian_slice11EndianSliceNtNtB9_9endianity12LittleEndianEjENtNtCs2ubz5g4KsBC_4core5clone5Clone5cloneCs1mKxMHUCrv1_3std' not inlined into '_RINvNtCshKUFdDGFpab_9addr2line4line11render_fileINtNtNtCsk2wPHUzPKJU_5gimli4read12endian_slice11EndianSliceNtNtBR_9endianity12LittleEndianEECs1mKxMHUCrv1_3std' because too costly to inline (cost=465, threshold=325)\n  function.rs:250: '_RNvMse_NtNtCsk2wPHUzPKJU_5gimli4read4unitINtB5_9AttributeINtNtB7_12endian_slice11EndianSliceNtNtB9_9endianity12LittleEndianEE5valueCs1mKxMHUCrv1_3std' not inlined into '_RNvMNtCshKUFdDGFpab_9addr2line4unitINtB2_7ResUnitINtNtNtCsk2wPHUzPKJU_5gimli4read12endian_slice11EndianSliceNtNtBS_9endianity12LittleEndianEE25find_function_or_locationCs1mKxMHUCrv1_3std' because too costly to inline (cost=5700, threshold=250)\n  function.rs:250: '_RNvNtNtNtNtCs1mKxMHUCrv1_3std12backtrace_rs9symbolize5gimli3elf15locate_build_id' not inlined into '_RINvMs0_NtNtNtCs1mKxMHUCrv1_3std12backtrace_rs9symbolize5gimliNtB6_5Cache11with_globalNCNvB6_7resolves_0EBc_' because too costly to inline (cost=1185, threshold=5)\n  function.rs:250: '_RNCNvNtNtCs1mKxMHUCrv1_3std3sys9backtrace10__print_fmts_0B7_' not inlined into '_RNSNvYNCNvNtNtCs1mKxMHUCrv1_3std3sys9backtrace10__print_fmts_0INtNtNtCs2ubz5g4KsBC_4core3ops8function6FnOnceTRNtNtNtBc_12backtrace_rs9backtrace5FrameEE9call_once6vtableBc_' because too costly to inline (cost=440, threshold=325)\n  function.rs:250: '_RNCINvMs0_NtNtCs1mKxMHUCrv1_3std4sync4onceNtB8_4Once9call_onceNCNvNtBc_2rt7cleanup0E0Bc_' not inlined into '_RNSNvYNCINvMs0_NtNtCs1mKxMHUCrv1_3std4sync4onceNtBd_4Once9call_onceNCNvNtBh_2rt7cleanup0E0INtNtNtCs2ubz5g4KsBC_4core3ops8function6FnOnceTRNtBd_9OnceStateEE9call_once6vtableBh_' because too costly to inline (cost=1015, threshold=325)\n  function.rs:250: '_RINvNtCs1mKxMHUCrv1_3std2rt15handle_rt_paniciEB4_' not inlined into '_RNvNtCs1mKxMHUCrv1_3std2rt19lang_start_internal' because too costly to inline (cost=85, threshold=0)\n  function.rs:250: '_RNCNvMs2_NtCs2rbbnA1oNun_10env_logger3fmtNtB7_7Builder5build0B9_' not inlined into '_RNSNvYNCNvMs2_NtCs2rbbnA1oNun_10env_logger3fmtNtBc_7Builder5build0INtNtNtCs2ubz5g4KsBC_4core3ops8function6FnOnceTQNtBc_9FormatterRNtCsiCfnkNipMYv_3log6RecordEE9call_once6vtableBe_' because too costly to inline (cost=510, threshold=45)\n  function.rs:250: '_RINvNtCs1mKxMHUCrv1_3std2rt15handle_rt_panicuEB4_' not inlined into '_RNvNvNtNtNtNtCs1mKxMHUCrv1_3std3sys12thread_local5guard3key6enable3run' because too costly to inline (cost=85, threshold=0)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q8\n\nsite kind      one marked function (function attribute)\nfunction       <core::io::write::default_write_fmt::Adapter<alloc::io::buffered::bufwriter::BufWriter<std::io::stdio::StdoutLock>> as core::fmt::Write>::write_str\nprofile share  2.45% of the program's user cycles\nsize after LTO 42 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:391: '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write9write_allBa_' not inlined into '_RNvYINtNvNtNtCs2ubz5g4KsBC_4core2io5write17default_write_fmt7AdapterNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrENtNtBb_3fmt5Write9write_fmtB1c_' because too costly to inline (cost=320, threshold=250)\n  write.rs:391: '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write9write_allBa_' not inlined into '_RNvXNvNtNtCs2ubz5g4KsBC_4core2io5write17default_write_fmtINtB2_7AdapterNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrENtNtB8_3fmt5Write9write_strB1f_' because too costly to inline (cost=320, threshold=250)\n  write.rs:391: '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write9write_allBa_' not inlined into '_RNvYINtNvNtNtCs2ubz5g4KsBC_4core2io5write17default_write_fmt7AdapterNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrENtNtBb_3fmt5Write10write_charB1c_' because too costly to inline (cost=320, threshold=250)\n  write.rs:391: '_RNvYNtNtCs2rbbnA1oNun_10env_logger3fmt9FormatterNtNtNtCs2ubz5g4KsBC_4core2io5write5Write9write_allB6_' not inlined into '_RNvXNvNtNtCs2ubz5g4KsBC_4core2io5write17default_write_fmtINtB2_7AdapterNtNtCs2rbbnA1oNun_10env_logger3fmt9FormatterENtNtB8_3fmt5Write9write_strB1b_' because too costly to inline (cost=95, threshold=45)\n  write.rs:391: '_RNvYNtNvMs4_NtCs2rbbnA1oNun_10env_logger3fmtNtBa_13DefaultFormat10write_args13IndentWrapperNtNtNtCs2ubz5g4KsBC_4core2io5write5Write9write_allBc_' not inlined into '_RNvXNvNtNtCs2ubz5g4KsBC_4core2io5write17default_write_fmtINtB2_7AdapterNtNvMs4_NtCs2rbbnA1oNun_10env_logger3fmtNtB1f_13DefaultFormat10write_args13IndentWrapperENtNtB8_3fmt5Write9write_strB1h_' because too costly to inline (cost=295, threshold=45)\n  write.rs:391: '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write9write_all' not inlined into '_RNvYINtNvNtNtCs2ubz5g4KsBC_4core2io5write17default_write_fmt7AdapterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtBb_3fmt5Write10write_charB1a_' because too costly to inline (cost=590, threshold=250)\n  write.rs:391: '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write9write_all' not inlined into '_RNvXNvNtNtCs2ubz5g4KsBC_4core2io5write17default_write_fmtINtB2_7AdapterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtB8_3fmt5Write9write_strB1d_' because too costly to inline (cost=590, threshold=250)\n  write.rs:391: '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write9write_all' not inlined into '_RNvYINtNvNtNtCs2ubz5g4KsBC_4core2io5write17default_write_fmt7AdapterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtBb_3fmt5Write9write_fmtB1a_' because too costly to inline (cost=590, threshold=250)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q9\n\nsite kind      one marked function (function attribute)\nfunction       <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap::map::core::get_hash<jaq_json::Val, jaq_json::Val>::{closure#0}>\n               (1 closure(s) defined inside it keep their own attributes)\nprofile share  2.43% of the program's user cycles, 2.43% counting the code inlined into it\nsize after LTO 392 LLVM instructions, 0 loop(s) inside it\nattributes now noinline,inlinehint,cold\n\nsource:\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hashbrown-0.15.5/src/raw/mod.rs:977 (lines 937-1017, the site's own line marked `>`)\n  937                  // SAFETY: The [`RawTableInner`] must already have properly initialized control\n  938                  // bytes since we will never expose RawTable::new_uninitialized in a public API.\n  939                  if self\n  940                      .reserve_rehash(additional, hasher, Fallibility::Infallible)\n  941                      .is_err()\n  942                  {\n  943                      // SAFETY: All allocation errors will be caught inside `RawTableInner::reserve_rehash`.\n  944                      hint::unreachable_unchecked()\n  945                  }\n  946              }\n  947          }\n  948      }\n  949  \n  950      /// Tries to ensure that at least `additional` items can be inserted into\n  951      /// the table without reallocation.\n  952      #[cfg_attr(feature = \"inline-more\", inline)]\n  953      pub fn try_reserve(\n  954          &mut self,\n  955          additional: usize,\n  956          hasher: impl Fn(&T) -> u64,\n  957      ) -> Result<(), TryReserveError> {\n  958          if additional > self.table.growth_left {\n  959              // SAFETY: The [`RawTableInner`] must already have properly initialized control\n  960              // bytes since we will never expose RawTable::new_uninitialized in a public API.\n  961              unsafe { self.reserve_rehash(additional, hasher, Fallibility::Fallible) }\n  962          } else {\n  963              Ok(())\n  964          }\n  965      }\n  966  \n  967      /// Out-of-line slow path for `reserve` and `try_reserve`.\n  968      ///\n  969      /// # Safety\n  970      ///\n  971      /// The [`RawTableInner`] must have properly initialized control bytes,\n  972      /// otherwise calling this function results in [`undefined behavior`]\n  973      ///\n  974      /// [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html\n  975      #[cold]\n  976      #[inline(never)]\n  977 >    unsafe fn reserve_rehash(\n  978          &mut self,\n  979          additional: usize,\n  980          hasher: impl Fn(&T) -> u64,\n  981          fallibility: Fallibility,\n  982      ) -> Result<(), TryReserveError> {\n  983          unsafe {\n  984              // SAFETY:\n  985              // 1. We know for sure that `alloc` and `layout` matches the [`Allocator`] and\n  986              //    [`TableLayout`] that were used to allocate this table.\n  987              // 2. The `drop` function is the actual drop function of the elements stored in\n  988              //    the table.\n  989              // 3. The caller ensures that the control bytes of the `RawTableInner`\n  990              //    are already initialized.\n  991              self.table.reserve_rehash_inner(\n  992                  &self.alloc,\n  993                  additional,\n  994                  &|table, index| hasher(table.bucket::<T>(index).as_ref()),\n  995                  fallibility,\n  996                  Self::TABLE_LAYOUT,\n  997                  if T::NEEDS_DROP {\n  998                      Some(|ptr| ptr::drop_in_place(ptr as *mut T))\n  999                  } else {\n 1000                      None\n 1001                  },\n 1002              )\n 1003          }\n 1004      }\n 1005  \n 1006      /// Allocates a new table of a different size and moves the contents of the\n 1007      /// current table into it.\n 1008      ///\n 1009      /// # Safety\n 1010      ///\n 1011      /// The [`RawTableInner`] must have properly initialized control bytes,\n 1012      /// otherwise calling this function results in [`undefined behavior`]\n 1013      ///\n 1014      /// The caller of this function must ensure that `capacity >= self.table.items`\n 1015      /// otherwise:\n 1016      ///\n 1017      /// * If `self.table.items != 0`, calling of this function with `capacity`\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:977: eliminating argument (4)\n  mod.rs:977: removing return value 1\n  mod.rs:977: List vectorization was possible but not beneficial with cost 0 >= 0\n  mod.rs:977: failed to move load with loop-invariant address because the loop may invalidate its value\n  mod.rs:977: load of type i64 not eliminated\n  mod.rs:977: load of type i8 not eliminated\n  mod.rs:977: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  mod.rs:977: loop not vectorized: value that could not be identified as reduction is used outside the loop\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q10\n\nsite kind      one marked function (function attribute)\nfunction       <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_json::Val, foldhash::fast::RandomState>>>::drop_slow\nprofile share  2.37% of the program's user cycles\nsize after LTO 291 LLVM instructions, 0 loop(s) inside it\nattributes now noinline,inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=1555612)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=430538)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=685644)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=662228)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=1597310)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=912158)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=1177153)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=838611)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q11\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q12\n\nsite kind      one marked function (function attribute)\nfunction       jaq_core::path::run\n               12 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  1.31% of the program's user cycles, 1.73% counting the code inlined into it\nsize after LTO 867 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/path.rs:99 (lines 59-139, the site's own line marked `>`)\n   59  impl<'a, U: Clone + 'a> Path<U> {\n   60      fn combinations<I, F>(self, mut iter: I) -> BoxIter<'a, Self>\n   61      where\n   62          I: Iterator<Item = (Part<F>, Opt)> + Clone + 'a,\n   63          F: IntoIterator<Item = U> + Clone + 'a,\n   64      {\n   65          if let Some((part, opt)) = iter.next() {\n   66              let parts = part.into_iter();\n   67              flat_map_with(parts, (self, iter), move |part, (mut prev, iter)| {\n   68                  prev.0.push((part, opt));\n   69                  prev.combinations(iter)\n   70              })\n   71          } else {\n   72              box_once(self)\n   73          }\n   74      }\n   75  }\n   76  \n   77  impl<'a, V: ValT + 'a> Path<V> {\n   78      pub(crate) fn run(self, v: V) -> ValRs<'a, V> {\n   79          run(self.0.into_iter(), v, |part, v| part.run(v))\n   80      }\n   81  \n   82      pub(crate) fn paths(self, vp: (V, RcList<V>)) -> ValRs<'a, (V, RcList<V>), V> {\n   83          run(self.0.into_iter(), vp, |part, vp| part.paths(vp))\n   84      }\n   85  \n   86      pub(crate) fn update<F>(mut self, v: V, f: F) -> ValX<'a, V>\n   87      where\n   88          F: Fn(V) -> ValXs<'a, V>,\n   89      {\n   90          if let Some(last) = self.0.pop() {\n   91              update(self.0.into_iter(), last, v, &f)\n   92          } else {\n   93              // should be unreachable\n   94              Ok(v)\n   95          }\n   96      }\n   97  }\n   98  \n   99 >fn run<'a, V: 'a, T: 'a>(\n  100      mut iter: impl Iterator<Item = (Part<V>, Opt)> + Clone + 'a,\n  101      val: T,\n  102      f: fn(Part<V>, T) -> ValRs<'a, T, V>,\n  103  ) -> ValRs<'a, T, V> {\n  104      if let Some((part, opt)) = iter.next() {\n  105          let essential = matches!(opt, Opt::Essential);\n  106          let ys = f(part, val).filter(move |v| essential || v.is_ok());\n  107          flat_map_with(ys, iter, move |v, iter| then(v, |v| run(iter, v, f)))\n  108      } else {\n  109          box_once(Ok(val))\n  110      }\n  111  }\n  112  \n  113  fn update<'a, V: ValT + 'a, P, F>(mut iter: P, last: (Part<V>, Opt), v: V, f: &F) -> ValX<'a, V>\n  114  where\n  115      P: Iterator<Item = (Part<V>, Opt)> + Clone,\n  116      F: Fn(V) -> ValXs<'a, V>,\n  117  {\n  118      if let Some((part, opt)) = iter.next() {\n  119          use core::iter::once;\n  120          part.update(v, opt, |v| once(update(iter.clone(), last.clone(), v, f)))\n  121      } else {\n  122          last.0.update(v, last.1, f)\n  123      }\n  124  }\n  125  \n  126  impl<'a, V: ValT + 'a> Part<V> {\n  127      fn run(&self, v: V) -> ValRs<'a, V> {\n  128          match self {\n  129              Self::Index(idx) => box_once(v.index(idx)),\n  130              Self::Range(None, None) => Box::new(v.values()),\n  131              Self::Range(from, upto) => box_once(v.range(from.as_ref()..upto.as_ref())),\n  132          }\n  133      }\n  134  \n  135      fn paths(&self, (v, p): (V, RcList<V>)) -> ValRs<'a, (V, RcList<V>), V> {\n  136          match self {\n  137              Self::Index(idx) => box_once(v.index(idx).map(|v| (v, p.cons(idx.clone())))),\n  138              Self::Range(None, None) => Box::new(\n  139                  v.key_values()\n\nwhat LLVM said about this region in the baseline build:\n  path.rs:99: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core4path3runNtCslKRKzoMO8pS_8jaq_json3ValBA_INtNtNtCsecSPvMcbw1R_5alloc3vec9into_iter8IntoIterTINtB2_4PartBA_ENtB2_3OptEEECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  path.rs:99: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core4path3runNtCslKRKzoMO8pS_8jaq_json3ValTBA_INtNtB4_7rc_list4ListBA_EEINtNtNtCsecSPvMcbw1R_5alloc3vec9into_iter8IntoIterTINtB2_4PartBA_ENtB2_3OptEEECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  path.rs:99: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core4path3runNtCslKRKzoMO8pS_8jaq_json3ValBA_INtNtNtCsecSPvMcbw1R_5alloc3vec9into_iter8IntoIterTINtB2_4PartBA_ENtB2_3OptEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  path.rs:96: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueINtNtCsecSPvMcbw1R_5alloc3vec3VecTINtNtCs3t0tOJGAbYW_8jaq_core4path4PartNtCslKRKzoMO8pS_8jaq_json3ValENtB1c_3OptEEECstofKklK7j5_7jaq_all' inlined into '_RNCINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtBa_7compile6TermId6updateNtNtCstofKklK7j5_7jaq_all4data8DataKindEs0_0B1b_' with (cost=105, threshold=525) at callsite _RINvMs2_NtCs3t0tOJGAbYW_8jaq_core4pathINtB6_4PathNtCslKRKzoMO8pS_8jaq_json3ValE6updateRINtNtCsecSPvMcbw1R_5alloc5boxed3BoxDINtNtB8_6filter6UpdateBL_Ep6OutputIB1o_DNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB2J_6result6ResultBL_INtNtB8_3exn3ExnBL_EEEL_EEL_EECstofKklK7j5_7jaq_all:10:5 @ _RNCNCINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtBc_7compile6TermId6updateNtNtCstofKklK7j5_7jaq_all4data8DataKindEs0_00B1d_:0:66 @ _RNCINvNtNtNtCs2ubz5g4KsBC_4core4iter8adapters3map12map_try_foldINtNtCs3t0tOJGAbYW_8jaq_core4path4PathINtNtBa_6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB14_3exn3ExnB1X_EEEIB1C_IB10_B1X_EB2q_EB1X_B1B_NvMs7_B12_BZ_9transposeNCNCINvMs5_NtB14_6filterNtNtB14_7compile6TermId6updateNtNtCstofKklK7j5_7jaq_all4data8DataKindEs0_00E0B4z_:0:21 @ _RINvYINtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtCs3t0tOJGAbYW_8jaq_core4path4PathINtNtBL_6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB1J_3exn3ExnB2C_EEEEL_EBD_8try_foldB2C_NCINvNtNtBJ_8adapters3map12map_try_foldB1E_IB2h_IB1F_B2C_EB35_EB2C_B2g_NvMs7_B1H_B1E_9transposeNCNCINvMs5_NtB1J_6filterNtNtB1J_7compile6TermId6updateNtNtCstofKklK7j5_7jaq_all4data8DataKindEs0_00E0B2g_EB6g_:8:21 @ _RINvXs0_NtNtNtCs2ubz5g4KsBC_4core4iter8adapters3mapINtB6_3MapINtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtBa_6traits8iterator8Iteratorp4ItemINtNtCs3t0tOJGAbYW_8jaq_core4path4PathINtNtBc_6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB2g_3exn3ExnB39_EEEEL_ENvMs7_B2e_B2b_9transposeEB1x_8try_foldB39_NCNCINvMs5_NtB2g_6filterNtNtB2g_7compile6TermId6updateNtNtCstofKklK7j5_7jaq_all4data8DataKindEs0_00B2N_EB5G_:6:19 @ _RNCINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtBa_7compile6TermId6updateNtNtCstofKklK7j5_7jaq_all4data8DataKindEs0_0B1b_:2:36;\n  path.rs:96: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueINtNtCsecSPvMcbw1R_5alloc3vec3VecTINtNtCs3t0tOJGAbYW_8jaq_core4path4PartNtCslKRKzoMO8pS_8jaq_json3ValENtB1c_3OptEEECsayvNaZMR3fG_3jaq' not inlined into '_RNCINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtBa_7compile6TermId6updateNtNtCstofKklK7j5_7jaq_all4data8DataKindEs0_0CsayvNaZMR3fG_3jaq' because too costly to inline (cost=105, threshold=45)\n  path.rs:94: loop not vectorized: call instruction cannot be vectorized\n  path.rs:106: Cannot promote indirect call: target with md5sum 0 not found (count=1555636)\n  path.rs:106: failed to move load with loop-invariant address because the loop may invalidate its value\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q13\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_json::Val as core::hash::Hash>::hash\n               12 monomorphizations of it exist in this build; the attribute goes on all of them\n               (8 closure(s) defined inside it keep their own attributes)\nprofile share  1.17% of the program's user cycles\nsize after LTO 1868 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/lib.rs:766 (lines 726-800, the site's own line marked `>`)\n  726                      let vr = r.iter().map(|(_k, v)| v);\n  727                      kl.cmp(kr).then_with(|| vl.cmp(vr))\n  728                  }\n  729              },\n  730  \n  731              // nulls are smaller than anything else\n  732              (Self::Null, _) => Less,\n  733              (_, Self::Null) => Greater,\n  734              // bools are smaller than anything else, except for nulls\n  735              (Self::Bool(_), _) => Less,\n  736              (_, Self::Bool(_)) => Greater,\n  737              // numbers are smaller than anything else, except for nulls and bools\n  738              (Self::Num(_), _) => Less,\n  739              (_, Self::Num(_)) => Greater,\n  740              // etc.\n  741              (Self::BStr(_) | Self::TStr(_), _) => Less,\n  742              (_, Self::BStr(_) | Self::TStr(_)) => Greater,\n  743              (Self::Arr(_), _) => Less,\n  744              (_, Self::Arr(_)) => Greater,\n  745          }\n  746      }\n  747  }\n  748  \n  749  impl PartialEq for Val {\n  750      fn eq(&self, other: &Self) -> bool {\n  751          match (self, other) {\n  752              (Self::Null, Self::Null) => true,\n  753              (Self::Bool(x), Self::Bool(y)) => x == y,\n  754              (Self::Num(x), Self::Num(y)) => x == y,\n  755              (Self::BStr(x) | Self::TStr(x), Self::BStr(y) | Self::TStr(y)) => x == y,\n  756              (Self::Arr(x), Self::Arr(y)) => x == y,\n  757              (Self::Obj(x), Self::Obj(y)) => x == y,\n  758              _ => false,\n  759          }\n  760      }\n  761  }\n  762  \n  763  impl Eq for Val {}\n  764  \n  765  impl Hash for Val {\n  766 >    fn hash<H: Hasher>(&self, state: &mut H) {\n  767          fn hash_with(u: u8, x: impl Hash, state: &mut impl Hasher) {\n  768              state.write_u8(u);\n  769              x.hash(state)\n  770          }\n  771          match self {\n  772              Self::Num(n) => n.hash(state),\n  773              // Num::hash() starts its hash with a 0 or 1, so we start with 2 here\n  774              Self::Null => state.write_u8(2),\n  775              Self::Bool(b) => state.write_u8(if *b { 3 } else { 4 }),\n  776              Self::BStr(b) | Self::TStr(b) => hash_with(5, b, state),\n  777              Self::Arr(a) => hash_with(6, a, state),\n  778              Self::Obj(o) => {\n  779                  state.write_u8(7);\n  780                  // this is similar to what happens in `Val::cmp`\n  781                  let mut kvs: Vec<_> = o.iter().collect();\n  782                  kvs.sort_by_key(|(k, _v)| *k);\n  783                  kvs.iter().for_each(|(k, v)| (k, v).hash(state));\n  784              }\n  785          }\n  786      }\n  787  }\n  788  \n  789  /// Display bytes as valid UTF-8 string.\n  790  ///\n  791  /// This maps invalid UTF-8 to the Unicode replacement character.\n  792  pub fn bstr(s: &(impl core::convert::AsRef<[u8]> + ?Sized)) -> impl fmt::Display + '_ {\n  793      BStr::new(s)\n  794  }\n  795  \n  796  impl fmt::Display for Val {\n  797      fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {\n  798          write::format(f, &write::Pp::default(), 0, self)\n  799      }\n  800  }\n\nwhat LLVM said about this region in the baseline build:\n  lib.rs:765: '_RNvMs1_Csle7Pmw50Tn_9xmlparserNtB5_9Tokenizer17parse_external_id' not inlined into '_RNvMs1_Csle7Pmw50Tn_9xmlparserNtB5_9Tokenizer15parse_next_impl' because too costly to inline (cost=1315, threshold=45)\n  lib.rs:764: '_RINvMs0_Cs1uapzJrW2gL_7memmap2NtB6_11MmapOptions3mapRNtNtCs1mKxMHUCrv1_3std2fs4FileECsayvNaZMR3fG_3jaq' not inlined into '_RNvXNtNtCs2ubz5g4KsBC_4core4iter8adaptersINtB2_12GenericShuntINtNtB2_5chain5ChainIBY_IBY_INtNtB2_3map3MapINtNtNtB6_5slice4iter4IterTNtNtCsecSPvMcbw1R_5alloc6string6StringB26_EENCNvCsayvNaZMR3fG_3jaq5binds0EIB1q_IB1G_TB26_NtNtNtCs1mKxMHUCrv1_3std3ffi6os_str8OsStringEENCB2Q_s0_0EEIB1q_B3n_NCB2Q_s1_0EEIB1q_B1F_NCB2Q_s_0EEINtNtB6_6result6ResultzNtB2S_5ErrorEENtNtNtB4_6traits8iterator8Iterator4nextB2S_' because too costly to inline (cost=225, threshold=45)\n  lib.rs:764: loop not vectorized: Loop contains an unsupported terminator\n  lib.rs:762: '_RNvMs0_NtCsle7Pmw50Tn_9xmlparser6streamNtB5_6Stream12consume_name' not inlined into '_RNvMs1_Csle7Pmw50Tn_9xmlparserNtB5_9Tokenizer15parse_next_impl' because too costly to inline (cost=195, threshold=45)\n  lib.rs:762: '_RNvMs2_NtCsl4DjnabWhvd_9rustyline4editINtB5_5StateuE11edit_insertCsayvNaZMR3fG_3jaq' not inlined into '_RNvMs3_Csl4DjnabWhvd_9rustylineINtB5_6EditoruNtNtB5_7history11FileHistoryE13readline_editCsayvNaZMR3fG_3jaq' because too costly to inline (cost=615, threshold=45)\n  lib.rs:761: '_RNvMs0_NtCsle7Pmw50Tn_9xmlparser6streamNtB5_6Stream14consume_spaces' not inlined into '_RNvMs1_Csle7Pmw50Tn_9xmlparserNtB5_9Tokenizer15parse_next_impl' because too costly to inline (cost=200, threshold=45)\n  lib.rs:761: '_RNvXs3_NtNtCsl4DjnabWhvd_9rustyline3tty4unixNtB5_14PosixRawReaderNtB7_9RawReader9next_char' not inlined into '_RNvMs3_Csl4DjnabWhvd_9rustylineINtB5_6EditoruNtNtB5_7history11FileHistoryE13readline_editCsayvNaZMR3fG_3jaq' because too costly to inline (cost=755, threshold=45)\n  lib.rs:771: '_RNvMs0_NtCsle7Pmw50Tn_9xmlparser6streamNtB5_6Stream12gen_text_pos' not inlined into '_RNvMs1_Csle7Pmw50Tn_9xmlparserNtB5_9Tokenizer15parse_next_impl' because it should never be inlined (cost=never): noinline function attribute\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n### q14\n\nsite kind      one marked function (function attribute)\nfunction       <&alloc::string::String as core::fmt::Display>::fmt\n               7 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  1.10% of the program's user cycles\nsize after LTO 354 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "d2a87092a6b62246", "site_map": {"q0": "fn:jaq_json::read::parse", "q1": "fn:<hifijson::SliceLexer as hifijson::token::Lex>::seq", "q10": "fn:<alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_json::Val, foldhash::fast::RandomState>>>::drop_slow", "q11": "fn:<jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once", "q12": "fn:jaq_core::path::run", "q13": "fn:<jaq_json::Val as core::hash::Hash>::hash", "q14": "fn:<&alloc::string::String as core::fmt::Display>::fmt", "q2": "fn:<hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold", "q3": "fn:<hifijson::SliceLexer as hifijson::write::Write>::write_until", "q4": "fn:<jaq_core::compile::TermId>::run", "q5": "fn:jaq_json::write::write", "q6": "fn:<jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once", "q7": "fn:<<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as core::ops::function::FnOnce<(jaq_core::path::Part<jaq_json::Val>, jaq_json::Val)>>::call_once", "q8": "fn:<core::io::write::default_write_fmt::Adapter<alloc::io::buffered::bufwriter::BufWriter<std::io::stdio::StdoutLock>> as core::fmt::Write>::write_str", "q9": "fn:<hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap::map::core::get_hash<jaq_json::Val, jaq_json::Val>::{closure#0}>"}, "stage": null, "tag": "V0-phaseA", "variant": "V0"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q10": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q10` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q11": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q11` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q12": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q12` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q13": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q13` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q14": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q14` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q15": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q15` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q5": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q5` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q6": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q6` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q7": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q7` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q8": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q8` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q9": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q9` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}}, "state": "# jev-opt hint search --- state format state-v1-2026-09-22, vocabulary v1-2026-09-22\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nPicking KEEP_DEFAULT everywhere reproduces the baseline exactly. A hint is\nworth choosing only if it is likely to make the WHOLE PROGRAM measurably\nfaster: the measurement is end-to-end wall time over the case set below, the\nnoise floor of this machine is about 1% aggregated, and nothing smaller than\n3% counts as a change. Hints that change the program's output are\nrejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (16 of them)\n\nEach section below is named after its question.\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 2)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      137 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q2\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       validations.rs:165 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  11\nloop body      116 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  validations.rs:164: load of type i8 not eliminated\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       read.rs:140 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  4\nloop body      442 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::token::Lex>::seq\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:140: _RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq has uninlinable pattern (recursive) and cost is not fully computed\n  read.rs:140: '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): recursive\n  read.rs:140: _RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_ has uninlinable pattern (recursive) and cost is not fully computed\n  read.rs:140: '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): recursive\n  read.rs:140: _RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all has uninlinable pattern (recursive) and cost is not fully computed\n  read.rs:140: '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): recursive\n  read.rs:140: _RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts is recursive and allocates too much stack space. Cost is not fully computed\n  read.rs:140: '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): recursive and allocates too much stack space\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q5\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 1)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  16\nloop body      58 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q6\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       count.rs:75 (loop nesting depth 1)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  16\nloop body      62 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  count.rs:75: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  count.rs:85: Vectorized horizontal reduction with cost -64 and with tree size 11\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q7\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q8\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q9\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      137 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q10\n\nsite kind      one loop inside a marked function\nmarked function <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_json::Val, foldhash::fast::RandomState>>>::drop_slow\nowner after inlining  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_json::Val, foldhash::fast::RandomState>>>::drop_slow\nlocation       mod.rs:848 (loop nesting depth 1)\nprofile share of the marked function  2.37%\naverage trip count (from the PGO profile)  4\nloop body      180 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=1555612)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=430538)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=685644)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=662228)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=1597310)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=912158)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=1177153)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=838611)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q11\n\nsite kind      one loop inside a marked function\nmarked function <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       utf8.rs:127 (loop nesting depth 1)\nprofile share of the marked function  2.71%\naverage trip count (from the PGO profile)  8\nloop body      220 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  utf8.rs:127: advising against unrolling the loop because it contains a call\n  utf8.rs:130: load of type i8 not eliminated\n  utf8.rs:123: '_RNvNtCs2ubz5g4KsBC_4core9panicking18panic_bounds_check' not inlined into '_RINvNtCs7XvlSH3kU6m_11regex_bites4utf86decodeRShEB4_' because it should never be inlined (cost=never): noinline function attribute\n  utf8.rs:131: '_RNvNtCsifSg31dUhSV_4bstr5ascii20first_non_ascii_byte' not inlined into '_RNvXs_NtCsifSg31dUhSV_4bstr4utf8NtB4_5CharsNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iterator5count' because too costly to inline (cost=365, threshold=45)\n  utf8.rs:131: '_RNvNtCsifSg31dUhSV_4bstr5ascii20first_non_ascii_byte' inlined into '_RNvMNtCslKRKzoMO8pS_8jaq_json4funsNtB4_3Val6length' with (cost=always): benefit over cost at callsite _RNvXs_NtCsifSg31dUhSV_4bstr4utf8NtB4_5CharsNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iterator5count:6:32 @ _RNvMNtCslKRKzoMO8pS_8jaq_json4funsNtB4_3Val6length:4:52;\n  utf8.rs:131: '_RNvNtCsifSg31dUhSV_4bstr5ascii20first_non_ascii_byte' inlined into '_RNvXs1i_NtCs2ubz5g4KsBC_4core3fmtRNtNtCsifSg31dUhSV_4bstr4bstr4BStrNtB6_7Display3fmtCstofKklK7j5_7jaq_all' with (cost=365, threshold=919) at callsite _RNvXs_NtCsifSg31dUhSV_4bstr4utf8NtB4_5CharsNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iterator5count:6:32 @ _RNvXNtNtCsifSg31dUhSV_4bstr5impls4bstrNtNtB6_4bstr4BStrNtNtCs2ubz5g4KsBC_4core3fmt7Display3fmt:29:43 @ _RNvXs1i_NtCs2ubz5g4KsBC_4core3fmtRNtNtCsifSg31dUhSV_4bstr4bstr4BStrNtB6_7Display3fmtCstofKklK7j5_7jaq_all:0:62;\n  utf8.rs:131: '_RNvNtCsifSg31dUhSV_4bstr5ascii20first_non_ascii_byte' not inlined into '_RNvXs0_NtNtNtCs2ubz5g4KsBC_4core4iter8adapters10filter_mapINtB5_9FilterMapINtNtB7_3zip3ZipNtNtCs7XvlSH3kU6m_11regex_bites5bytes17SubCaptureMatchesNtB1s_12CaptureNamesENCNvNtCs8murSGRldJT_7jaq_std5regex5regex0ENtNtNtB9_6traits8iterator8Iterator4nextB2L_' because too costly to inline (cost=365, threshold=45)\n  utf8.rs:131: '_RNvNtCsifSg31dUhSV_4bstr5ascii20first_non_ascii_byte' inlined into '_RNvXNtNtCsifSg31dUhSV_4bstr5impls4bstrNtNtB6_4bstr4BStrNtNtCs2ubz5g4KsBC_4core3fmt7Display3fmt' with (cost=365, threshold=919) at callsite _RNvXs_NtCsifSg31dUhSV_4bstr4utf8NtB4_5CharsNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iterator5count:6:32 @ _RNvXNtNtCsifSg31dUhSV_4bstr5impls4bstrNtNtB6_4bstr4BStrNtNtCs2ubz5g4KsBC_4core3fmt7Display3fmt:29:43;\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q12\n\nsite kind      one loop inside a marked function\nmarked function <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       utf8.rs:640 (loop nesting depth 2)\nprofile share of the marked function  2.71%\naverage trip count (from the PGO profile)  2\nloop body      32 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  utf8.rs:640: loop not vectorized: loop control flow is not understood by vectorizer\n  utf8.rs:640: loop not vectorized: Incorrect number of successors from early exiting block\n  utf8.rs:640: loop not vectorized\n  utf8.rs:644: loop not vectorized: Loop contains an unsupported switch\n  utf8.rs:635: load of type i8 not eliminated\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q13\n\nsite kind      one loop inside a marked function\nmarked function <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       ascii.rs:208 (loop nesting depth 2)\nprofile share of the marked function  2.71%\naverage trip count (from the PGO profile)  7\nloop body      7 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  ascii.rs:208: loop not vectorized: unable to calculate the loop count due to complex control flow\n  ascii.rs:208: the cost-model indicates that vectorization is not beneficial\n  ascii.rs:208: the cost-model indicates that interleaving is not beneficial\n  ascii.rs:207: loop not vectorized: unable to calculate the loop count due to complex control flow\n  ascii.rs:207: the cost-model indicates that vectorization is not beneficial\n  ascii.rs:207: the cost-model indicates that interleaving is not beneficial\n  ascii.rs:207: unrolled loop by a factor of 8 with run-time trip count\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q14\n\nsite kind      one loop inside a marked function\nmarked function <jaq_core::compile::TermId>::run\nowner after inlining  <jaq_core::compile::TermId>::run::<jaq_all::data::DataKind>\nlocation       mod.rs:4148 (loop nesting depth 1)\nprofile share of the marked function  8.32%\naverage trip count (from the PGO profile)  127\nloop body      74 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:4148: '_RNvXNtNtCs2ubz5g4KsBC_4core4iter8adaptersINtB2_12GenericShuntINtNtB2_3map3MapNtNtNtB6_3str4iter5BytesNCNvNtCslKRKzoMO8pS_8jaq_json3num22biguint_from_str_radix0EINtNtB6_6option6OptionzEENtNtNtB4_6traits8iterator8Iterator4nextB1H_' not inlined into '_RNvMNtCslKRKzoMO8pS_8jaq_json3numNtB2_3Num14from_str_radix' because too costly to inline (cost=60, threshold=45)\n  mod.rs:4148: '_RNvXNtNtCs2ubz5g4KsBC_4core4iter8adaptersINtB2_12GenericShuntINtNtB2_3map3MapINtNtNtB4_7sources7from_fn6FromFnNCNvNtCslKRKzoMO8pS_8jaq_json4read10parse_manys_0EINvCsJiC9gTn727_8jaq_fmts16map_invalid_dataNtB1Q_3ValNtB1O_5ErrorEEINtNtB6_6result6ResultzNtNtNtB6_2io5error5ErrorEENtNtNtB4_6traits8iterator8Iterator4nextCsayvNaZMR3fG_3jaq' not inlined into '_RINvNtNtCs2ubz5g4KsBC_4core4iter8adapters11try_processINtNtB2_3map3MapINtNtNtB4_7sources7from_fn6FromFnNCNvNtCslKRKzoMO8pS_8jaq_json4read10parse_manys_0EINvCsJiC9gTn727_8jaq_fmts16map_invalid_dataNtB1J_3ValNtB1H_5ErrorEEB38_INtNtB6_6result6ResultzNtNtNtB6_2io5error5ErrorENCINvXso_B3D_IB3B_B38_B3X_EINtNtNtB4_6traits7collect12FromIteratorB4z_E9from_iterBQ_E0B38_ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=305, threshold=45)\n  mod.rs:4148: '_RNvXNtNtCs2ubz5g4KsBC_4core4iter8adaptersINtB2_12GenericShuntINtNtNtB4_7sources7from_fn6FromFnNCNvMs1_NtNtCs3t0tOJGAbYW_8jaq_core4load5parseNtB1C_6Parser4defss_0EINtNtB6_6result6ResultzTINtB1C_6ExpectReEINtNtB6_6option6OptionRINtNtB1E_3lex5TokenB3c_EEEEENtNtNtB4_6traits8iterator8Iterator4nextB1G_' inlined into '_RNvMs1_NtNtCs3t0tOJGAbYW_8jaq_core4load5parseNtB5_6Parser4defs' with (cost=-14835, threshold=250) at callsite _RINvMsk_NtCsecSPvMcbw1R_5alloc3vecINtB6_3VecINtNtNtCs3t0tOJGAbYW_8jaq_core4load5parse3DefReEE16extend_desugaredINtNtNtCs2ubz5g4KsBC_4core4iter8adapters12GenericShuntINtNtNtB1Q_7sources7from_fn6FromFnNCNvMs1_BJ_NtBJ_6Parser4defss_0EINtNtB1S_6result6ResultzTINtBJ_6ExpectB1p_EINtNtB1S_6option6OptionRINtNtBL_3lex5TokenB1p_EEEEEEBN_:8:44 @ _RNvXNtNtCsecSPvMcbw1R_5alloc3vec11spec_extendINtB4_3VecINtNtNtCs3t0tOJGAbYW_8jaq_core4load5parse3DefReEEINtB2_10SpecExtendBR_INtNtNtCs2ubz5g4KsBC_4core4iter8adapters12GenericShuntINtNtNtB24_7sources7from_fn6FromFnNCNvMs1_BU_NtBU_6Parser4defss_0EINtNtB26_6result6ResultzTINtBU_6ExpectB1A_EINtNtB26_6option6OptionRINtNtBW_3lex5TokenB1A_EEEEEE11spec_extendBY_:1:14 @ _RNvXNtNtCsecSPvMcbw1R_5alloc3vec21spec_from_iter_nestedINtB4_3VecINtNtNtCs3t0tOJGAbYW_8jaq_core4load5parse3DefReEEINtB2_18SpecFromIterNestedB11_INtNtNtCs2ubz5g4KsBC_4core4iter8adapters12GenericShuntINtNtNtB2n_7sources7from_fn6FromFnNCNvMs1_B14_NtB14_6Parser4defss_0EINtNtB2p_6result6ResultzTINtB14_6ExpectB1K_EINtNtB2p_6option6OptionRINtNtB16_3lex5TokenB1K_EEEEEE9from_iterB18_:23:9 @ _RNvXNtNtCsecSPvMcbw1R_5alloc3vec14spec_from_iterINtB4_3VecINtNtNtCs3t0tOJGAbYW_8jaq_core4load5parse3DefReEEINtB2_12SpecFromIterBU_INtNtNtCs2ubz5g4KsBC_4core4iter8adapters12GenericShuntINtNtNtB29_7sources7from_fn6FromFnNCNvMs1_BX_NtBX_6Parser4defss_0EINtNtB2b_6result6ResultzTINtBX_6ExpectB1D_EINtNtB2b_6option6OptionRINtNtBZ_3lex5TokenB1D_EEEEEE9from_iterB11_:1:9 @ _RINvXsf_NtCsecSPvMcbw1R_5alloc3vecINtB6_3VecINtNtNtCs3t0tOJGAbYW_8jaq_core4load5parse3DefReEEINtNtNtNtCs2ubz5g4KsBC_4core4iter6traits7collect12FromIteratorBG_E9from_iterINtNtB1A_8adapters12GenericShuntINtNtNtB1A_7sources7from_fn6FromFnNCNvMs1_BJ_NtBJ_6Parser4defss_0EINtNtB1C_6result6ResultzTINtBJ_6ExpectB1p_EINtNtB1C_6option6OptionRINtNtBL_3lex5TokenB1p_EEEEEEBN_:1:9 @ _RINvYINtNtNtCs2ubz5g4KsBC_4core4iter8adapters12GenericShuntINtNtNtB8_7sources7from_fn6FromFnNCNvMs1_NtNtCs3t0tOJGAbYW_8jaq_core4load5parseNtB1A_6Parser4defss_0EINtNtBa_6result6ResultzTINtB1A_6ExpectReEINtNtBa_6option6OptionRINtNtB1C_3lex5TokenB3a_EEEEENtNtNtB8_6traits8iterator8Iterator7collectINtNtCsecSPvMcbw1R_5alloc3vec3VecINtB1A_3DefB3a_EEEB1E_:13:9 @ _RNCINvXso_NtCs2ubz5g4KsBC_4core6resultINtB8_6ResultINtNtCsecSPvMcbw1R_5alloc3vec3VecINtNtNtCs3t0tOJGAbYW_8jaq_core4load5parse3DefReEETINtB1n_6ExpectB23_EINtNtBa_6option6OptionRINtNtB1p_3lex5TokenB23_EEEEINtNtNtNtBa_4iter6traits7collect12FromIteratorIBB_B1k_B27_EE9from_iterINtNtNtB3m_7sources7from_fn6FromFnNCNvMs1_B1n_NtB1n_6Parser4defss_0EE0B1r_:0:51 @ _RINvNtNtCs2ubz5g4KsBC_4core4iter8adapters11try_processINtNtNtB4_7sources7from_fn6FromFnNCNvMs1_NtNtCs3t0tOJGAbYW_8jaq_core4load5parseNtB1v_6Parser4defss_0EINtB1v_3DefReEINtNtB6_6result6ResultzTINtB1v_6ExpectB2E_EINtNtB6_6option6OptionRINtNtB1x_3lex5TokenB2E_EEEENCINvXso_B2K_IB2I_INtNtCsecSPvMcbw1R_5alloc3vec3VecB2t_EB34_EINtNtNtB4_6traits7collect12FromIteratorIB2I_B2t_B34_EE9from_iterBQ_E0B4u_EB1z_:11:17 @ _RINvXso_NtCs2ubz5g4KsBC_4core6resultINtB6_6ResultINtNtCsecSPvMcbw1R_5alloc3vec3VecINtNtNtCs3t0tOJGAbYW_8jaq_core4load5parse3DefReEETINtB1l_6ExpectB21_EINtNtB8_6option6OptionRINtNtB1n_3lex5TokenB21_EEEEINtNtNtNtB8_4iter6traits7collect12FromIteratorIBz_B1i_B25_EE9from_iterINtNtNtB3k_7sources7from_fn6FromFnNCNvMs1_B1l_NtB1l_6Parser4defss_0EEB1p_:1:9 @ _RINvYINtNtNtNtCs2ubz5g4KsBC_4core4iter7sources7from_fn6FromFnNCNvMs1_NtNtCs3t0tOJGAbYW_8jaq_core4load5parseNtB15_6Parser4defss_0ENtNtNtBa_6traits8iterator8Iterator7collectINtNtBc_6result6ResultINtNtCsecSPvMcbw1R_5alloc3vec3VecINtB15_3DefReEETINtB15_6ExpectB3N_EINtNtBc_6option6OptionRINtNtB17_3lex5TokenB3N_EEEEEB19_:13:9 @ _RNvMs1_NtNtCs3t0tOJGAbYW_8jaq_core4load5parseNtB5_6Parser4defs:2:75;\n  mod.rs:4148: '_RNvXNtNtCs2ubz5g4KsBC_4core4iter8adaptersINtB2_12GenericShuntINtNtNtB4_7sources7from_fn6FromFnNCINvMs1_NtNtCs3t0tOJGAbYW_8jaq_core4load5parseNtB1D_6Parser6moduleINtB1D_4TermReENCNCNvB1F_10parse_mains_00Es_0EINtNtB6_6result6ResultzTINtB1D_6ExpectB2L_EINtNtB6_6option6OptionRINtNtB1F_3lex5TokenB2L_EEEEENtNtNtB4_6traits8iterator8Iterator4nextB1H_' not inlined into '_RNvNtCs3t0tOJGAbYW_8jaq_core4load10parse_main' because too costly to inline (cost=670, threshold=45)\n  mod.rs:4148: '_RNvXNtNtCs2ubz5g4KsBC_4core4iter8adaptersINtB2_12GenericShuntINtNtNtB4_7sources7from_fn6FromFnNCINvMs1_NtNtCs3t0tOJGAbYW_8jaq_core4load5parseNtB1D_6Parser6moduleINtNtCsecSPvMcbw1R_5alloc3vec3VecINtB1D_3DefReEENCNCNvB1F_10parse_defss_00Es_0EINtNtB6_6result6ResultzTINtB1D_6ExpectB3h_EINtNtB6_6option6OptionRINtNtB1F_3lex5TokenB3h_EEEEENtNtNtB4_6traits8iterator8Iterator4nextB1H_' not inlined into '_RNvNtCs3t0tOJGAbYW_8jaq_core4load10parse_defs' because too costly to inline (cost=670, threshold=45)\n  mod.rs:4148: '_RNvXs0_NtNtNtCs2ubz5g4KsBC_4core4iter8adapters10filter_mapINtB5_9FilterMapINtNtB7_3zip3ZipNtNtCs7XvlSH3kU6m_11regex_bites5bytes17SubCaptureMatchesNtB1s_12CaptureNamesENCNvNtCs8murSGRldJT_7jaq_std5regex5regex0ENtNtNtB9_6traits8iterator8Iterator4nextB2L_' not inlined into '_RNvNtCs8murSGRldJT_7jaq_std5regex5regex' because too costly to inline (cost=740, threshold=45)\n  mod.rs:4148: '_RNvXNtNtCs2ubz5g4KsBC_4core4iter8adaptersINtB2_12GenericShuntINtNtB2_7flatten7FlatMapINtNtNtCsecSPvMcbw1R_5alloc3vec9into_iter8IntoIterNtCslKRKzoMO8pS_8jaq_json3ValEINtNtB1s_5boxed3BoxDNtNtNtB4_6traits8iterator8Iteratorp4ItemINtNtB6_6result6ResultB29_INtNtCs3t0tOJGAbYW_8jaq_core3exn3ExnB29_EEEL_ENCINvNtB46_6filter14recurse_updateB29_E0EIB3C_zB41_EEB2X_4nextCstofKklK7j5_7jaq_all' not inlined into '_RINvXs_CslKRKzoMO8pS_8jaq_jsonNtB5_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT10map_valuesINtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB25_6result6ResultBs_INtNtBF_3exn3ExnBs_EEEL_ENCINvNtBF_6filter14recurse_updateBs_E0ECstofKklK7j5_7jaq_all' because too costly to inline (cost=980, threshold=45)\n  mod.rs:4148: '_RNvXNtNtCs2ubz5g4KsBC_4core4iter8adaptersINtB2_12GenericShuntINtNtB2_7flatten7FlatMapINtNtNtCsecSPvMcbw1R_5alloc3vec9into_iter8IntoIterNtCslKRKzoMO8pS_8jaq_json3ValEINtNtNtB4_7sources4once4OnceINtNtB6_6result6ResultB29_INtNtCs3t0tOJGAbYW_8jaq_core3exn3ExnB29_EEENCINvNtB3A_4path6updateB29_IB1m_TINtB4h_4PartB29_ENtB4h_3OptEERINtNtB1s_5boxed3BoxDINtNtB3A_6filter6UpdateB29_Ep6OutputIB5e_DNtNtNtB4_6traits8iterator8Iteratorp4ItemB35_EL_EEL_EE0EIB36_zB3v_EEB6d_4nextCstofKklK7j5_7jaq_all' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core4path6updateNtCslKRKzoMO8pS_8jaq_json3ValINtNtNtCsecSPvMcbw1R_5alloc3vec9into_iter8IntoIterTINtB2_4PartBD_ENtB2_3OptEERINtNtB1d_5boxed3BoxDINtNtB4_6filter6UpdateBD_Ep6OutputIB2n_DNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB3s_6result6ResultBD_INtNtB4_3exn3ExnBD_EEEL_EEL_EECstofKklK7j5_7jaq_all' because too costly to inline (cost=495, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n### q15\n\nsite kind      one loop inside a marked function\nmarked function <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_json::Val, foldhash::fast::RandomState>>>::drop_slow\nowner after inlining  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_json::Val, foldhash::fast::RandomState>>>::drop_slow\nlocation       mod.rs:848 (loop nesting depth 1)\nprofile share of the marked function  2.37%\naverage trip count (from the PGO profile)  28\nloop body      6 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=1555612)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=430538)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=685644)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=662228)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=1597310)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=912158)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=1177153)\n  mod.rs:848: Cannot promote indirect call: target with md5sum 0 not found (count=838611)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n"}, "sha": "279385bc6764362c", "site_map": {"q0": "<&alloc::string::String as core::fmt::Display>::fmt@macros.rs:180:28#d2", "q1": "jaq_json::write::write@mod.rs:1653:12#d2", "q10": "<alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_json::Val, foldhash::fast::RandomState>>>::drop_slow@mod.rs:848:1#d1", "q11": "<jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once@utf8.rs:127:9#d1", "q12": "<jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once@utf8.rs:640:11#d2", "q13": "<jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once@ascii.rs:208:12#d2", "q14": "<jaq_core::compile::TermId>::run@mod.rs:4148:9#d1", "q15": "<alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_json::Val, foldhash::fast::RandomState>>>::drop_slow@mod.rs:848:1#d1~2", "q2": "jaq_json::read::parse@validations.rs:165:12#d1", "q3": "jaq_json::read::parse@read.rs:140:27#d1", "q4": "jaq_json::read::parse@str.rs:194:13#d1", "q5": "<&alloc::string::String as core::fmt::Display>::fmt@macros.rs:180:28#d1", "q6": "<&alloc::string::String as core::fmt::Display>::fmt@count.rs:75:5#d1", "q7": "<jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once@macros.rs:279:24#d1", "q8": "jaq_json::write::write@mod.rs:1653:12#d1", "q9": "jaq_json::write::write@mod.rs:1653:12#d1~2"}, "stage": null, "tag": "V0-phaseB", "variant": "V0"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Leave this function's attributes exactly as they are. LLVM's own inlining cost model and the default 16-byte function alignment decide, as they do in the baseline build.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which function attribute should the build put on it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the attribute to make the whole program measurably faster.", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V0b, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nPicking KEEP_DEFAULT everywhere reproduces the baseline exactly. A hint is\nworth choosing only if it is likely to make the WHOLE PROGRAM measurably\nfaster: the measurement is end-to-end wall time over the case set below, the\nnoise floor of this machine is about 1% aggregated, and nothing smaller than\n3% counts as a change. Hints that change the program's output are\nrejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "35ccdad4823d3c3d", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V0b-F1-F2-F3-F4", "variant": "V0b"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. LLVM's vectoriser and unroller decide with their own cost model, as in the baseline build.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function, together with the function attributes this build has already applied. Which loop hint should the build attach to it? Pick KEEP_DEFAULT unless there is a reason in that section to expect the hint to make the whole program measurably faster.", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V0b, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nPicking KEEP_DEFAULT everywhere reproduces the baseline exactly. A hint is\nworth choosing only if it is likely to make the WHOLE PROGRAM measurably\nfaster: the measurement is end-to-end wall time over the case set below, the\nnoise floor of this machine is about 1% aggregated, and nothing smaller than\n3% counts as a change. Hints that change the program's output are\nrejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "aa8b9d688473eb27", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V0b-L1-L2-L3-L4-L5", "variant": "V0b"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V1, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nAt each site in this request the build **will** apply one of the listed\nhints. The question is which one. The measurement is end-to-end wall time\nover the case set below; the noise floor of this machine is about 1%\naggregated. Hints that change the program's output are rejected regardless of\nspeed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "5145f964e5208032", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V1-F1-F2-F3-F4", "variant": "V1"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V1, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nAt each site in this request the build **will** apply one of the listed\nhints. The question is which one. The measurement is end-to-end wall time\nover the case set below; the noise floor of this machine is about 1%\naggregated. Hints that change the program's output are rejected regardless of\nspeed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "999849664dc1c4e7", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V1-L1-L2-L3-L4-L5", "variant": "V1"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V2, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "a418cb81b15131aa", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V2-F1-F2-F3-F4", "variant": "V2"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V2, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "43bd3f5398f3be74", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V2-L1-L2-L3-L4-L5", "variant": "V2"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V3, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n"}, "sha": "befc0ce383bb4167", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V3-F1-F2-F3-F4", "variant": "V3"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V3, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n"}, "sha": "e53204a450c6e17f", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V3-L1-L2-L3-L4-L5", "variant": "V3"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V4, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "4604b12092c4b25c", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V4-F1-F2-F3-F4", "variant": "V4"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V4, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "7b119b32e48ce6ff", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V4-L1-L2-L3-L4-L5", "variant": "V4"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0_s1": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `inline` to it (Add the `inlinehint` attribute (the SPEC vocabulary's `inline`))?", "type": "score"}, "q0_s2": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `inline(never)` to it (Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`))?", "type": "score"}, "q0_s3": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `cold` to it (Add the `cold` attribute)?", "type": "score"}, "q0_s4": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `align=16` to it (Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`))?", "type": "score"}, "q0_s5": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `align=32` to it (Set the function's alignment to 32 bytes (`align=32`))?", "type": "score"}, "q0_s6": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `align=64` to it (Set the function's alignment to 64 bytes (`align=64`))?", "type": "score"}, "q0_worth": {"instructions": "Section `q0` of the state describes one site of this program. Is this site worth spending a hint on at all, given its share of the program's cycles, its shape, and the candidates available for it?", "type": "noul"}, "q1_s1": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `inline` to it (Add the `inlinehint` attribute (the SPEC vocabulary's `inline`))?", "type": "score"}, "q1_s2": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `inline(never)` to it (Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`))?", "type": "score"}, "q1_s3": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `cold` to it (Add the `cold` attribute)?", "type": "score"}, "q1_s4": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `align=16` to it (Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`))?", "type": "score"}, "q1_s5": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `align=32` to it (Set the function's alignment to 32 bytes (`align=32`))?", "type": "score"}, "q1_s6": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `align=64` to it (Set the function's alignment to 64 bytes (`align=64`))?", "type": "score"}, "q1_worth": {"instructions": "Section `q1` of the state describes one site of this program. Is this site worth spending a hint on at all, given its share of the program's cycles, its shape, and the candidates available for it?", "type": "noul"}, "q2_s1": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `inline` to it (Add the `inlinehint` attribute (the SPEC vocabulary's `inline`))?", "type": "score"}, "q2_s2": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `inline(never)` to it (Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`))?", "type": "score"}, "q2_s3": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `cold` to it (Add the `cold` attribute)?", "type": "score"}, "q2_s4": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `align=16` to it (Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`))?", "type": "score"}, "q2_s5": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `align=32` to it (Set the function's alignment to 32 bytes (`align=32`))?", "type": "score"}, "q2_s6": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `align=64` to it (Set the function's alignment to 64 bytes (`align=64`))?", "type": "score"}, "q2_worth": {"instructions": "Section `q2` of the state describes one site of this program. Is this site worth spending a hint on at all, given its share of the program's cycles, its shape, and the candidates available for it?", "type": "noul"}, "q3_s1": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `inline` to it (Add the `inlinehint` attribute (the SPEC vocabulary's `inline`))?", "type": "score"}, "q3_s2": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `inline(never)` to it (Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`))?", "type": "score"}, "q3_s3": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `cold` to it (Add the `cold` attribute)?", "type": "score"}, "q3_s4": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `align=16` to it (Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`))?", "type": "score"}, "q3_s5": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `align=32` to it (Set the function's alignment to 32 bytes (`align=32`))?", "type": "score"}, "q3_s6": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `align=64` to it (Set the function's alignment to 64 bytes (`align=64`))?", "type": "score"}, "q3_worth": {"instructions": "Section `q3` of the state describes one site of this program. Is this site worth spending a hint on at all, given its share of the program's cycles, its shape, and the candidates available for it?", "type": "noul"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V5, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "b4e58a070fe1328c", "site_map": {"q0": "F1", "q0_s1": "F1/inline", "q0_s2": "F1/inline_never", "q0_s3": "F1/cold", "q0_s4": "F1/align_16", "q0_s5": "F1/align_32", "q0_s6": "F1/align_64", "q0_worth": "F1/worth", "q1": "F2", "q1_s1": "F2/inline", "q1_s2": "F2/inline_never", "q1_s3": "F2/cold", "q1_s4": "F2/align_16", "q1_s5": "F2/align_32", "q1_s6": "F2/align_64", "q1_worth": "F2/worth", "q2": "F3", "q2_s1": "F3/inline", "q2_s2": "F3/inline_never", "q2_s3": "F3/cold", "q2_s4": "F3/align_16", "q2_s5": "F3/align_32", "q2_s6": "F3/align_64", "q2_worth": "F3/worth", "q3": "F4", "q3_s1": "F4/inline", "q3_s2": "F4/inline_never", "q3_s3": "F4/cold", "q3_s4": "F4/align_16", "q3_s5": "F4/align_32", "q3_s6": "F4/align_64", "q3_worth": "F4/worth"}, "stage": null, "tag": "V5-F1-F2-F3-F4", "variant": "V5"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0_s1": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=2` to it (Attach `llvm)?", "type": "score"}, "q0_s10": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=2` to it (Attach `llvm)?", "type": "score"}, "q0_s11": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=4` to it (Attach `llvm)?", "type": "score"}, "q0_s2": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=4` to it (Attach `llvm)?", "type": "score"}, "q0_s3": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=8` to it (Attach `llvm)?", "type": "score"}, "q0_s4": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.disable` to it (Attach `llvm)?", "type": "score"}, "q0_s5": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=2` to it (Attach `llvm)?", "type": "score"}, "q0_s6": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=4` to it (Attach `llvm)?", "type": "score"}, "q0_s7": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=8` to it (Attach `llvm)?", "type": "score"}, "q0_s8": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=16` to it (Attach `llvm)?", "type": "score"}, "q0_s9": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q0` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=1` to it (Attach `llvm)?", "type": "score"}, "q0_worth": {"instructions": "Section `q0` of the state describes one site of this program. Is this site worth spending a hint on at all, given its share of the program's cycles, its shape, and the candidates available for it?", "type": "noul"}, "q1_s1": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=2` to it (Attach `llvm)?", "type": "score"}, "q1_s10": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=2` to it (Attach `llvm)?", "type": "score"}, "q1_s11": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=4` to it (Attach `llvm)?", "type": "score"}, "q1_s2": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=4` to it (Attach `llvm)?", "type": "score"}, "q1_s3": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=8` to it (Attach `llvm)?", "type": "score"}, "q1_s4": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.disable` to it (Attach `llvm)?", "type": "score"}, "q1_s5": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=2` to it (Attach `llvm)?", "type": "score"}, "q1_s6": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=4` to it (Attach `llvm)?", "type": "score"}, "q1_s7": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=8` to it (Attach `llvm)?", "type": "score"}, "q1_s8": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=16` to it (Attach `llvm)?", "type": "score"}, "q1_s9": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q1` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=1` to it (Attach `llvm)?", "type": "score"}, "q1_worth": {"instructions": "Section `q1` of the state describes one site of this program. Is this site worth spending a hint on at all, given its share of the program's cycles, its shape, and the candidates available for it?", "type": "noul"}, "q2_s1": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=2` to it (Attach `llvm)?", "type": "score"}, "q2_s10": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=2` to it (Attach `llvm)?", "type": "score"}, "q2_s11": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=4` to it (Attach `llvm)?", "type": "score"}, "q2_s2": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=4` to it (Attach `llvm)?", "type": "score"}, "q2_s3": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=8` to it (Attach `llvm)?", "type": "score"}, "q2_s4": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.disable` to it (Attach `llvm)?", "type": "score"}, "q2_s5": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=2` to it (Attach `llvm)?", "type": "score"}, "q2_s6": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=4` to it (Attach `llvm)?", "type": "score"}, "q2_s7": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=8` to it (Attach `llvm)?", "type": "score"}, "q2_s8": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=16` to it (Attach `llvm)?", "type": "score"}, "q2_s9": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q2` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=1` to it (Attach `llvm)?", "type": "score"}, "q2_worth": {"instructions": "Section `q2` of the state describes one site of this program. Is this site worth spending a hint on at all, given its share of the program's cycles, its shape, and the candidates available for it?", "type": "noul"}, "q3_s1": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=2` to it (Attach `llvm)?", "type": "score"}, "q3_s10": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=2` to it (Attach `llvm)?", "type": "score"}, "q3_s11": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=4` to it (Attach `llvm)?", "type": "score"}, "q3_s2": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=4` to it (Attach `llvm)?", "type": "score"}, "q3_s3": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=8` to it (Attach `llvm)?", "type": "score"}, "q3_s4": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.disable` to it (Attach `llvm)?", "type": "score"}, "q3_s5": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=2` to it (Attach `llvm)?", "type": "score"}, "q3_s6": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=4` to it (Attach `llvm)?", "type": "score"}, "q3_s7": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=8` to it (Attach `llvm)?", "type": "score"}, "q3_s8": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=16` to it (Attach `llvm)?", "type": "score"}, "q3_s9": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q3` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=1` to it (Attach `llvm)?", "type": "score"}, "q3_worth": {"instructions": "Section `q3` of the state describes one site of this program. Is this site worth spending a hint on at all, given its share of the program's cycles, its shape, and the candidates available for it?", "type": "noul"}, "q4_s1": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=2` to it (Attach `llvm)?", "type": "score"}, "q4_s10": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=2` to it (Attach `llvm)?", "type": "score"}, "q4_s11": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=4` to it (Attach `llvm)?", "type": "score"}, "q4_s2": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=4` to it (Attach `llvm)?", "type": "score"}, "q4_s3": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.count=8` to it (Attach `llvm)?", "type": "score"}, "q4_s4": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `unroll.disable` to it (Attach `llvm)?", "type": "score"}, "q4_s5": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=2` to it (Attach `llvm)?", "type": "score"}, "q4_s6": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=4` to it (Attach `llvm)?", "type": "score"}, "q4_s7": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=8` to it (Attach `llvm)?", "type": "score"}, "q4_s8": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `vectorize.width=16` to it (Attach `llvm)?", "type": "score"}, "q4_s9": {"criteria": ["No measurable change: under 3% on this site's own time, or an effect too small to survive the whole-program measurement.", "Small gain: 3-10% faster on this site.", "Clear gain: 10-50% faster on this site.", "Large gain: more than 50% faster on this site."], "instructions": "Section `q4` of the state describes one site of this program. How much faster would that site get if the build applied `interleave.count=1` to it (Attach `llvm)?", "type": "score"}, "q4_worth": {"instructions": "Section `q4` of the state describes one site of this program. Is this site worth spending a hint on at all, given its share of the program's cycles, its shape, and the candidates available for it?", "type": "noul"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V5, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "5874ab2a6f27411c", "site_map": {"q0": "L1", "q0_s1": "L1/unroll_count_2", "q0_s10": "L1/interleave_count_2", "q0_s11": "L1/interleave_count_4", "q0_s2": "L1/unroll_count_4", "q0_s3": "L1/unroll_count_8", "q0_s4": "L1/unroll_disable", "q0_s5": "L1/vectorize_width_2", "q0_s6": "L1/vectorize_width_4", "q0_s7": "L1/vectorize_width_8", "q0_s8": "L1/vectorize_width_16", "q0_s9": "L1/interleave_count_1", "q0_worth": "L1/worth", "q1": "L2", "q1_s1": "L2/unroll_count_2", "q1_s10": "L2/interleave_count_2", "q1_s11": "L2/interleave_count_4", "q1_s2": "L2/unroll_count_4", "q1_s3": "L2/unroll_count_8", "q1_s4": "L2/unroll_disable", "q1_s5": "L2/vectorize_width_2", "q1_s6": "L2/vectorize_width_4", "q1_s7": "L2/vectorize_width_8", "q1_s8": "L2/vectorize_width_16", "q1_s9": "L2/interleave_count_1", "q1_worth": "L2/worth", "q2": "L3", "q2_s1": "L3/unroll_count_2", "q2_s10": "L3/interleave_count_2", "q2_s11": "L3/interleave_count_4", "q2_s2": "L3/unroll_count_4", "q2_s3": "L3/unroll_count_8", "q2_s4": "L3/unroll_disable", "q2_s5": "L3/vectorize_width_2", "q2_s6": "L3/vectorize_width_4", "q2_s7": "L3/vectorize_width_8", "q2_s8": "L3/vectorize_width_16", "q2_s9": "L3/interleave_count_1", "q2_worth": "L3/worth", "q3": "L4", "q3_s1": "L4/unroll_count_2", "q3_s10": "L4/interleave_count_2", "q3_s11": "L4/interleave_count_4", "q3_s2": "L4/unroll_count_4", "q3_s3": "L4/unroll_count_8", "q3_s4": "L4/unroll_disable", "q3_s5": "L4/vectorize_width_2", "q3_s6": "L4/vectorize_width_4", "q3_s7": "L4/vectorize_width_8", "q3_s8": "L4/vectorize_width_16", "q3_s9": "L4/interleave_count_1", "q3_worth": "L4/worth", "q4": "L5", "q4_s1": "L5/unroll_count_2", "q4_s10": "L5/interleave_count_2", "q4_s11": "L5/interleave_count_4", "q4_s2": "L5/unroll_count_4", "q4_s3": "L5/unroll_count_8", "q4_s4": "L5/unroll_disable", "q4_s5": "L5/vectorize_width_2", "q4_s6": "L5/vectorize_width_4", "q4_s7": "L5/vectorize_width_8", "q4_s8": "L5/vectorize_width_16", "q4_s9": "L5/interleave_count_1", "q4_worth": "L5/worth"}, "stage": null, "tag": "V5-L1-L2-L3-L4-L5", "variant": "V5"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V6, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n  ... (26 earlier lines omitted)\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  ... (25 later lines omitted)\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  ... (30 earlier lines omitted)\n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  ... (30 later lines omitted)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  ... (23 earlier lines omitted)\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  ... (23 later lines omitted)\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "bf9c953ea52cff9c", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V6-F1-F2-F3-F4", "variant": "V6"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V6, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  ... (23 earlier lines omitted)\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  ... (23 later lines omitted)\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n  ... (26 earlier lines omitted)\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  ... (25 later lines omitted)\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  ... (10 earlier lines omitted)\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  ... (10 later lines omitted)\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "9905bb7c693ce01a", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V6-L1-L2-L3-L4-L5", "variant": "V6"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V7, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "5f706f5649ca3f85", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V7-F1-F2-F3-F4", "variant": "V7"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V7, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n"}, "sha": "39ac8317e7f4995f", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V7-L1-L2-L3-L4-L5", "variant": "V7"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V8, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 1.73% |\n  | perf reach share (this function anywhere in the inline frame stack) | 1.73% |\n  | share of the objsearch case | 0.0% |\n  | share of the strproc case | 4.1% |\n  | share of the readwrite case | 0.0% |\n  | size and shape after LTO | 251 LLVM insts (432 machine insts, 98 of them vector, 14 machine backedges), 1 monomorphization, 1 loop site inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | already vectorized (width 4, interleave 2 and 4) plus `completely unrolled` at macros.rs:279; cost model says vectorisation and interleaving are not beneficial |\n"}, "sha": "e988e9eddefb2414", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V8-F1-F2-F3-F4", "variant": "V8"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V8, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <&alloc::string::String as core::fmt::Display>::fmt |\n  | that function's perf share (all six workloads) | 1.10% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 5.6% |\n  | average trip count (PGO) | 32.0 |\n  | loop header execution count (PGO) | 722,491,314 |\n  | body size | 36 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 2 |\n  | loops this site key names | 1 (the key names exactly one loop) |\n  | hotness (header count x body instructions) | 2.60e10 --- the largest hotness of any loop_in_mark key in this program |\n  | inline chain from the marked function down to the loop | Formatter::pad -> Chars::count -> do_count_chars -> <slice::Iter<[u8;4]>>::next |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit with complex writes to memory; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) + unroller advises against (contains a call/invoke) + cost model says not beneficial |\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 25.8 |\n  | loop header execution count (PGO) | 144,524,134 |\n  | body size | 141 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 4 (one hint on this key lands on all four loops; their hotness sums to 3.24e10) |\n  | hotness (header count x body instructions) | 2.04e10 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> BufWriter<StdoutLock>::write_fmt -> default_write_fmt -> core::fmt::write |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit; loop induction variable could not be identified; could not determine number of loop iterations) + unroller advises against (contains a call/invoke) |\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <jaq_std::base_run<..>::{closure#7} as FnOnce<..>>::call_once |\n  | that function's perf share (all six workloads) | 1.73% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 4.1% |\n  | its share of the readwrite case | 0.0% |\n  | average trip count (PGO) | 221.7 |\n  | loop header execution count (PGO) | 177,080,637 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.30e9 |\n  | inline chain from the marked function down to the loop | base_run{closure#7} -> map_utf8_str -> ByteSlice::to_ascii_lowercase -> <slice::Iter<u8>>::next |\n  | what the baseline remarks on this loop's line say | LEGAL and already vectorized: `vectorized loop (vectorization width: 4, interleaved count: 2)` and `(width 4, interleaved count: 4)` at this line, plus `completely unrolled`; cost model reports vectorisation and interleaving as not beneficial, and one `runtime pointer checks needed`. Remarks are attributed by source line only, and several loops share macros.rs:279 |\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::read::parse |\n  | that function's perf share (all six workloads) | 29.29% |\n  | its share of the objsearch case | 33.9% |\n  | its share of the strproc case | 26.0% |\n  | its share of the readwrite case | 27.5% |\n  | average trip count (PGO) | 13.9 |\n  | loop header execution count (PGO) | 207,748,447 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.89e9 |\n  | inline chain from the marked function down to the loop | read::parse -> parse_string -> str_fold -> write_until -> Copied<Iter<u8>>::position -> position::check |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (loop contains an unsupported switch; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) |\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 1.0 |\n  | loop header execution count (PGO) | 5,591,880 |\n  | body size | 200 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 1.12e9 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> <slice::Iter<..>>::next |\n  | what the baseline remarks on this loop's line say | not recorded for this site (it is outside the frozen site set, so no remark attribution was run for it) |\n"}, "sha": "bb4290bac833ce48", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V8-L1-L2-L3-L4-L5", "variant": "V8"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V9, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n"}, "sha": "243cb5eb50bf3081", "site_map": {"q0": "F1"}, "stage": null, "tag": "V9-F1", "variant": "V9"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V9, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n"}, "sha": "cf1590b65b29bf6a", "site_map": {"q0": "F2"}, "stage": null, "tag": "V9-F2", "variant": "V9"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V9, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n"}, "sha": "35a85c06db019a0d", "site_map": {"q0": "F3"}, "stage": null, "tag": "V9-F3", "variant": "V9"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V9, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 1.73% |\n  | perf reach share (this function anywhere in the inline frame stack) | 1.73% |\n  | share of the objsearch case | 0.0% |\n  | share of the strproc case | 4.1% |\n  | share of the readwrite case | 0.0% |\n  | size and shape after LTO | 251 LLVM insts (432 machine insts, 98 of them vector, 14 machine backedges), 1 monomorphization, 1 loop site inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | already vectorized (width 4, interleave 2 and 4) plus `completely unrolled` at macros.rs:279; cost model says vectorisation and interleaving are not beneficial |\n"}, "sha": "ac126b3a69937b69", "site_map": {"q0": "F4"}, "stage": null, "tag": "V9-F4", "variant": "V9"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V9, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <&alloc::string::String as core::fmt::Display>::fmt |\n  | that function's perf share (all six workloads) | 1.10% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 5.6% |\n  | average trip count (PGO) | 32.0 |\n  | loop header execution count (PGO) | 722,491,314 |\n  | body size | 36 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 2 |\n  | loops this site key names | 1 (the key names exactly one loop) |\n  | hotness (header count x body instructions) | 2.60e10 --- the largest hotness of any loop_in_mark key in this program |\n  | inline chain from the marked function down to the loop | Formatter::pad -> Chars::count -> do_count_chars -> <slice::Iter<[u8;4]>>::next |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit with complex writes to memory; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) + unroller advises against (contains a call/invoke) + cost model says not beneficial |\n"}, "sha": "17635d3e63f0b6cf", "site_map": {"q0": "L1"}, "stage": null, "tag": "V9-L1", "variant": "V9"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V9, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 25.8 |\n  | loop header execution count (PGO) | 144,524,134 |\n  | body size | 141 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 4 (one hint on this key lands on all four loops; their hotness sums to 3.24e10) |\n  | hotness (header count x body instructions) | 2.04e10 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> BufWriter<StdoutLock>::write_fmt -> default_write_fmt -> core::fmt::write |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit; loop induction variable could not be identified; could not determine number of loop iterations) + unroller advises against (contains a call/invoke) |\n"}, "sha": "34b701a9c9f88040", "site_map": {"q0": "L2"}, "stage": null, "tag": "V9-L2", "variant": "V9"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V9, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <jaq_std::base_run<..>::{closure#7} as FnOnce<..>>::call_once |\n  | that function's perf share (all six workloads) | 1.73% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 4.1% |\n  | its share of the readwrite case | 0.0% |\n  | average trip count (PGO) | 221.7 |\n  | loop header execution count (PGO) | 177,080,637 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.30e9 |\n  | inline chain from the marked function down to the loop | base_run{closure#7} -> map_utf8_str -> ByteSlice::to_ascii_lowercase -> <slice::Iter<u8>>::next |\n  | what the baseline remarks on this loop's line say | LEGAL and already vectorized: `vectorized loop (vectorization width: 4, interleaved count: 2)` and `(width 4, interleaved count: 4)` at this line, plus `completely unrolled`; cost model reports vectorisation and interleaving as not beneficial, and one `runtime pointer checks needed`. Remarks are attributed by source line only, and several loops share macros.rs:279 |\n"}, "sha": "2c13020657a805e5", "site_map": {"q0": "L3"}, "stage": null, "tag": "V9-L3", "variant": "V9"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V9, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::read::parse |\n  | that function's perf share (all six workloads) | 29.29% |\n  | its share of the objsearch case | 33.9% |\n  | its share of the strproc case | 26.0% |\n  | its share of the readwrite case | 27.5% |\n  | average trip count (PGO) | 13.9 |\n  | loop header execution count (PGO) | 207,748,447 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.89e9 |\n  | inline chain from the marked function down to the loop | read::parse -> parse_string -> str_fold -> write_until -> Copied<Iter<u8>>::position -> position::check |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (loop contains an unsupported switch; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) |\n"}, "sha": "583da541d4874eaa", "site_map": {"q0": "L4"}, "stage": null, "tag": "V9-L4", "variant": "V9"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V9, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 1.0 |\n  | loop header execution count (PGO) | 5,591,880 |\n  | body size | 200 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 1.12e9 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> <slice::Iter<..>>::next |\n  | what the baseline remarks on this loop's line say | not recorded for this site (it is outside the frozen site set, so no remark attribution was run for it) |\n"}, "sha": "b876cb2a9bcf71cc", "site_map": {"q0": "L5"}, "stage": null, "tag": "V9-L5", "variant": "V9"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V10, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n"}, "sha": "4cbed5fd18c2ddf5", "site_map": {"q0": "F1"}, "stage": null, "tag": "V10-F1", "variant": "V10"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V10, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n"}, "sha": "92238add6b76bc6e", "site_map": {"q0": "F2"}, "stage": null, "tag": "V10-F2", "variant": "V10"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V10, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n"}, "sha": "d189181c5a920979", "site_map": {"q0": "F3"}, "stage": null, "tag": "V10-F3", "variant": "V10"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V10, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 1.73% |\n  | perf reach share (this function anywhere in the inline frame stack) | 1.73% |\n  | share of the objsearch case | 0.0% |\n  | share of the strproc case | 4.1% |\n  | share of the readwrite case | 0.0% |\n  | size and shape after LTO | 251 LLVM insts (432 machine insts, 98 of them vector, 14 machine backedges), 1 monomorphization, 1 loop site inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | already vectorized (width 4, interleave 2 and 4) plus `completely unrolled` at macros.rs:279; cost model says vectorisation and interleaving are not beneficial |\n"}, "sha": "75bf893bcaf45850", "site_map": {"q0": "F4"}, "stage": null, "tag": "V10-F4", "variant": "V10"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V10, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <&alloc::string::String as core::fmt::Display>::fmt |\n  | that function's perf share (all six workloads) | 1.10% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 5.6% |\n  | average trip count (PGO) | 32.0 |\n  | loop header execution count (PGO) | 722,491,314 |\n  | body size | 36 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 2 |\n  | loops this site key names | 1 (the key names exactly one loop) |\n  | hotness (header count x body instructions) | 2.60e10 --- the largest hotness of any loop_in_mark key in this program |\n  | inline chain from the marked function down to the loop | Formatter::pad -> Chars::count -> do_count_chars -> <slice::Iter<[u8;4]>>::next |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit with complex writes to memory; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) + unroller advises against (contains a call/invoke) + cost model says not beneficial |\n"}, "sha": "38c3730355313701", "site_map": {"q0": "L1"}, "stage": null, "tag": "V10-L1", "variant": "V10"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V10, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 25.8 |\n  | loop header execution count (PGO) | 144,524,134 |\n  | body size | 141 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 4 (one hint on this key lands on all four loops; their hotness sums to 3.24e10) |\n  | hotness (header count x body instructions) | 2.04e10 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> BufWriter<StdoutLock>::write_fmt -> default_write_fmt -> core::fmt::write |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit; loop induction variable could not be identified; could not determine number of loop iterations) + unroller advises against (contains a call/invoke) |\n"}, "sha": "38bd6f3191339bc6", "site_map": {"q0": "L2"}, "stage": null, "tag": "V10-L2", "variant": "V10"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V10, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <jaq_std::base_run<..>::{closure#7} as FnOnce<..>>::call_once |\n  | that function's perf share (all six workloads) | 1.73% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 4.1% |\n  | its share of the readwrite case | 0.0% |\n  | average trip count (PGO) | 221.7 |\n  | loop header execution count (PGO) | 177,080,637 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.30e9 |\n  | inline chain from the marked function down to the loop | base_run{closure#7} -> map_utf8_str -> ByteSlice::to_ascii_lowercase -> <slice::Iter<u8>>::next |\n  | what the baseline remarks on this loop's line say | LEGAL and already vectorized: `vectorized loop (vectorization width: 4, interleaved count: 2)` and `(width 4, interleaved count: 4)` at this line, plus `completely unrolled`; cost model reports vectorisation and interleaving as not beneficial, and one `runtime pointer checks needed`. Remarks are attributed by source line only, and several loops share macros.rs:279 |\n"}, "sha": "30232a2d8d9cd9ed", "site_map": {"q0": "L3"}, "stage": null, "tag": "V10-L3", "variant": "V10"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V10, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::read::parse |\n  | that function's perf share (all six workloads) | 29.29% |\n  | its share of the objsearch case | 33.9% |\n  | its share of the strproc case | 26.0% |\n  | its share of the readwrite case | 27.5% |\n  | average trip count (PGO) | 13.9 |\n  | loop header execution count (PGO) | 207,748,447 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.89e9 |\n  | inline chain from the marked function down to the loop | read::parse -> parse_string -> str_fold -> write_until -> Copied<Iter<u8>>::position -> position::check |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (loop contains an unsupported switch; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) |\n"}, "sha": "a44ea503b4844fdd", "site_map": {"q0": "L4"}, "stage": null, "tag": "V10-L4", "variant": "V10"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V10, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What this request is\n\nThis is round 1 of a search. Each answer becomes one entry of a build plan,\nthe plan is built once, and the result is measured against the baseline and\nrecorded. A hint that turns out not to help costs one build and is discarded;\nnothing here is shipped to anyone, and no answer has to be defended later.\nThe search only learns from arms that differ from the baseline: a round in\nwhich every site is left unchanged rebuilds the baseline and measures\nnothing. Where a hint has any mechanism at all, trying it is worth more than\nkeeping the default; where no candidate has a mechanism, saying so is still\nthe right answer.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (1 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 1.0 |\n  | loop header execution count (PGO) | 5,591,880 |\n  | body size | 200 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 1.12e9 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> <slice::Iter<..>>::next |\n  | what the baseline remarks on this loop's line say | not recorded for this site (it is outside the frozen site set, so no remark attribution was run for it) |\n"}, "sha": "0cbfcb891bfad4b0", "site_map": {"q0": "L5"}, "stage": null, "tag": "V10-L5", "variant": "V10"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V11, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What each hint in the list does, in general\n\nThis section is about the hints themselves. It says nothing about the sites\nin this request.\n\n`inline` (`inlinehint`)\n  Raises the inliner's threshold for one function, so callers that were just\n  over the limit now paste its body in. It pays on a small, hot function\n  called from few places, where the caller then specialises on constants it\n  passes. It backfires on a large body, on a function called from many\n  places (every copy costs instruction cache), and it is a no-op when the\n  function already carries the attribute or is already inlined everywhere.\n\n`inline(never)` (`noinline`)\n  Forces one out-of-line copy. It pays when a large body was being pasted\n  into many callers and the instruction cache, not the call overhead, is the\n  cost --- typically a big dispatch or a recursive body with many\n  monomorphizations. It costs a call and the lost specialisation at every\n  call site, so it hurts on a small hot leaf.\n\n`cold`\n  Tells LLVM the function is rarely executed: calls to it move out of line,\n  it is optimised for size, and it is never inlined. It is correct only for\n  error and fallback paths. On anything that carries a real share of the\n  cycles it is a straight pessimisation.\n\n`align=16 / 32 / 64`\n  Moves the function's entry to that boundary, which changes how its first\n  instructions and its first loop fall into the 32-byte fetch windows and the\n  uop cache. 16 is already the x86-64 default, so it mostly pins what is\n  there. Larger values can help a function whose hot loop sits near its\n  entry, they waste padding, and their effect is close to unpredictable ---\n  when alignment has been measured to matter it was usually as a whole-build\n  setting rather than one function's entry.\n\n`unroll.count = 2 / 4 / 8`\n  Overrides the unroller, including its own advice not to unroll. It pays on\n  a short body with a high trip count, where loop control and the dependence\n  chain between iterations are a real fraction of the work. It costs code\n  size and a longer remainder loop, so it hurts when the trip count is low,\n  when the body is already large, or when the body contains a call --- the\n  call dominates whatever the loop control cost. Note that unrolling runs\n  after vectorisation, so on a loop LLVM vectorises this unrolls the vector\n  loop.\n\n`unroll.disable`\n  Forbids unrolling. It pays when the caller's instruction footprint is the\n  problem, or when the loop is short and its trip count unpredictable.\n\n`vectorize.width = 2 / 4 / 8 / 16`\n  Forces the number of lanes per vector iteration and enables vectorisation.\n  The right width is a property of the element type and the machine's vector\n  register: lanes x element size should reach the register width, and going\n  past it makes LLVM emit several registers per iteration. It pays when the\n  loop is long, straight-line, has no calls, and LLVM chose a width below\n  what the element type allows. It is **not a permission slip**: when the\n  compiler reports the loop as illegal to vectorize --- an early exit, an\n  unsupported switch, a value that is not a recognised reduction escaping the\n  loop, an undeterminable trip count --- the hint is dropped and the build\n  is unchanged. It hurts when the trip count is too low to amortise the\n  remainder loop, and a width far above the register width has been measured\n  to produce code several times slower than the default.\n\n`interleave.count = 1 / 2 / 4`\n  How many independent vector bodies run per iteration. More chains hide the\n  latency of a dependent reduction at the cost of registers and a longer\n  remainder; `1` shortens the loop when the default interleaving was too\n  aggressive for the trip count.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 1.73% |\n  | perf reach share (this function anywhere in the inline frame stack) | 1.73% |\n  | share of the objsearch case | 0.0% |\n  | share of the strproc case | 4.1% |\n  | share of the readwrite case | 0.0% |\n  | size and shape after LTO | 251 LLVM insts (432 machine insts, 98 of them vector, 14 machine backedges), 1 monomorphization, 1 loop site inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | already vectorized (width 4, interleave 2 and 4) plus `completely unrolled` at macros.rs:279; cost model says vectorisation and interleaving are not beneficial |\n"}, "sha": "4eeab426becd217f", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V11-F1-F2-F3-F4", "variant": "V11"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V11, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## What each hint in the list does, in general\n\nThis section is about the hints themselves. It says nothing about the sites\nin this request.\n\n`inline` (`inlinehint`)\n  Raises the inliner's threshold for one function, so callers that were just\n  over the limit now paste its body in. It pays on a small, hot function\n  called from few places, where the caller then specialises on constants it\n  passes. It backfires on a large body, on a function called from many\n  places (every copy costs instruction cache), and it is a no-op when the\n  function already carries the attribute or is already inlined everywhere.\n\n`inline(never)` (`noinline`)\n  Forces one out-of-line copy. It pays when a large body was being pasted\n  into many callers and the instruction cache, not the call overhead, is the\n  cost --- typically a big dispatch or a recursive body with many\n  monomorphizations. It costs a call and the lost specialisation at every\n  call site, so it hurts on a small hot leaf.\n\n`cold`\n  Tells LLVM the function is rarely executed: calls to it move out of line,\n  it is optimised for size, and it is never inlined. It is correct only for\n  error and fallback paths. On anything that carries a real share of the\n  cycles it is a straight pessimisation.\n\n`align=16 / 32 / 64`\n  Moves the function's entry to that boundary, which changes how its first\n  instructions and its first loop fall into the 32-byte fetch windows and the\n  uop cache. 16 is already the x86-64 default, so it mostly pins what is\n  there. Larger values can help a function whose hot loop sits near its\n  entry, they waste padding, and their effect is close to unpredictable ---\n  when alignment has been measured to matter it was usually as a whole-build\n  setting rather than one function's entry.\n\n`unroll.count = 2 / 4 / 8`\n  Overrides the unroller, including its own advice not to unroll. It pays on\n  a short body with a high trip count, where loop control and the dependence\n  chain between iterations are a real fraction of the work. It costs code\n  size and a longer remainder loop, so it hurts when the trip count is low,\n  when the body is already large, or when the body contains a call --- the\n  call dominates whatever the loop control cost. Note that unrolling runs\n  after vectorisation, so on a loop LLVM vectorises this unrolls the vector\n  loop.\n\n`unroll.disable`\n  Forbids unrolling. It pays when the caller's instruction footprint is the\n  problem, or when the loop is short and its trip count unpredictable.\n\n`vectorize.width = 2 / 4 / 8 / 16`\n  Forces the number of lanes per vector iteration and enables vectorisation.\n  The right width is a property of the element type and the machine's vector\n  register: lanes x element size should reach the register width, and going\n  past it makes LLVM emit several registers per iteration. It pays when the\n  loop is long, straight-line, has no calls, and LLVM chose a width below\n  what the element type allows. It is **not a permission slip**: when the\n  compiler reports the loop as illegal to vectorize --- an early exit, an\n  unsupported switch, a value that is not a recognised reduction escaping the\n  loop, an undeterminable trip count --- the hint is dropped and the build\n  is unchanged. It hurts when the trip count is too low to amortise the\n  remainder loop, and a width far above the register width has been measured\n  to produce code several times slower than the default.\n\n`interleave.count = 1 / 2 / 4`\n  How many independent vector bodies run per iteration. More chains hide the\n  latency of a dependent reduction at the cost of registers and a longer\n  remainder; `1` shortens the loop when the default interleaving was too\n  aggressive for the trip count.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <&alloc::string::String as core::fmt::Display>::fmt |\n  | that function's perf share (all six workloads) | 1.10% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 5.6% |\n  | average trip count (PGO) | 32.0 |\n  | loop header execution count (PGO) | 722,491,314 |\n  | body size | 36 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 2 |\n  | loops this site key names | 1 (the key names exactly one loop) |\n  | hotness (header count x body instructions) | 2.60e10 --- the largest hotness of any loop_in_mark key in this program |\n  | inline chain from the marked function down to the loop | Formatter::pad -> Chars::count -> do_count_chars -> <slice::Iter<[u8;4]>>::next |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit with complex writes to memory; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) + unroller advises against (contains a call/invoke) + cost model says not beneficial |\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 25.8 |\n  | loop header execution count (PGO) | 144,524,134 |\n  | body size | 141 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 4 (one hint on this key lands on all four loops; their hotness sums to 3.24e10) |\n  | hotness (header count x body instructions) | 2.04e10 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> BufWriter<StdoutLock>::write_fmt -> default_write_fmt -> core::fmt::write |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit; loop induction variable could not be identified; could not determine number of loop iterations) + unroller advises against (contains a call/invoke) |\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <jaq_std::base_run<..>::{closure#7} as FnOnce<..>>::call_once |\n  | that function's perf share (all six workloads) | 1.73% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 4.1% |\n  | its share of the readwrite case | 0.0% |\n  | average trip count (PGO) | 221.7 |\n  | loop header execution count (PGO) | 177,080,637 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.30e9 |\n  | inline chain from the marked function down to the loop | base_run{closure#7} -> map_utf8_str -> ByteSlice::to_ascii_lowercase -> <slice::Iter<u8>>::next |\n  | what the baseline remarks on this loop's line say | LEGAL and already vectorized: `vectorized loop (vectorization width: 4, interleaved count: 2)` and `(width 4, interleaved count: 4)` at this line, plus `completely unrolled`; cost model reports vectorisation and interleaving as not beneficial, and one `runtime pointer checks needed`. Remarks are attributed by source line only, and several loops share macros.rs:279 |\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::read::parse |\n  | that function's perf share (all six workloads) | 29.29% |\n  | its share of the objsearch case | 33.9% |\n  | its share of the strproc case | 26.0% |\n  | its share of the readwrite case | 27.5% |\n  | average trip count (PGO) | 13.9 |\n  | loop header execution count (PGO) | 207,748,447 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.89e9 |\n  | inline chain from the marked function down to the loop | read::parse -> parse_string -> str_fold -> write_until -> Copied<Iter<u8>>::position -> position::check |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (loop contains an unsupported switch; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) |\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 1.0 |\n  | loop header execution count (PGO) | 5,591,880 |\n  | body size | 200 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 1.12e9 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> <slice::Iter<..>>::next |\n  | what the baseline remarks on this loop's line say | not recorded for this site (it is outside the frozen site set, so no remark attribution was run for it) |\n"}, "sha": "311dce17884dac65", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V11-L1-L2-L3-L4-L5", "variant": "V11"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"alignment": "A function alignment attribute (16, 32 or 64 bytes): move the function's entry point to a coarser boundary.", "cold": "The `cold` attribute: declare this function to be off the hot path, so callers move calls to it out of line and it is optimised for size.", "inlining": "An inlining attribute (`inlinehint` or `noinline`): change whether and how often this function's body is pasted into its callers.", "leave_alone": "No function attribute would help this function; leave it as the baseline has it."}, "instructions": "Section `q0` of the state describes one marked function of this program. Before choosing a value, choose a kind: which of these families of change, if any, has a mechanism that could make this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"alignment": "A function alignment attribute (16, 32 or 64 bytes): move the function's entry point to a coarser boundary.", "cold": "The `cold` attribute: declare this function to be off the hot path, so callers move calls to it out of line and it is optimised for size.", "inlining": "An inlining attribute (`inlinehint` or `noinline`): change whether and how often this function's body is pasted into its callers.", "leave_alone": "No function attribute would help this function; leave it as the baseline has it."}, "instructions": "Section `q1` of the state describes one marked function of this program. Before choosing a value, choose a kind: which of these families of change, if any, has a mechanism that could make this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"alignment": "A function alignment attribute (16, 32 or 64 bytes): move the function's entry point to a coarser boundary.", "cold": "The `cold` attribute: declare this function to be off the hot path, so callers move calls to it out of line and it is optimised for size.", "inlining": "An inlining attribute (`inlinehint` or `noinline`): change whether and how often this function's body is pasted into its callers.", "leave_alone": "No function attribute would help this function; leave it as the baseline has it."}, "instructions": "Section `q2` of the state describes one marked function of this program. Before choosing a value, choose a kind: which of these families of change, if any, has a mechanism that could make this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"alignment": "A function alignment attribute (16, 32 or 64 bytes): move the function's entry point to a coarser boundary.", "cold": "The `cold` attribute: declare this function to be off the hot path, so callers move calls to it out of line and it is optimised for size.", "inlining": "An inlining attribute (`inlinehint` or `noinline`): change whether and how often this function's body is pasted into its callers.", "leave_alone": "No function attribute would help this function; leave it as the baseline has it."}, "instructions": "Section `q3` of the state describes one marked function of this program. Before choosing a value, choose a kind: which of these families of change, if any, has a mechanism that could make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V12, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 1.73% |\n  | perf reach share (this function anywhere in the inline frame stack) | 1.73% |\n  | share of the objsearch case | 0.0% |\n  | share of the strproc case | 4.1% |\n  | share of the readwrite case | 0.0% |\n  | size and shape after LTO | 251 LLVM insts (432 machine insts, 98 of them vector, 14 machine backedges), 1 monomorphization, 1 loop site inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | already vectorized (width 4, interleave 2 and 4) plus `completely unrolled` at macros.rs:279; cost model says vectorisation and interleaving are not beneficial |\n"}, "sha": "4df5e2d36266d09a", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": 1, "tag": "V12-F1-F2-F3-F4-stage1", "variant": "V12"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"interleave": "An interleaving hint (`interleave.count = 1/2/4`): change how many independent vector chains run per iteration.", "leave_alone": "No loop hint would help this loop; leave it as the baseline has it.", "unroll": "An unrolling hint (`unroll.count = 2/4/8` or `unroll.disable`): change how many copies of the body run per iteration, overriding the unroller's own decision.", "vectorize": "A vectorisation width hint (`vectorize.width = 2/4/8/16`): force the number of lanes per vector iteration, and enable vectorisation."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Before choosing a value, choose a kind: which of these families of hint, if any, has a mechanism that could make this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"interleave": "An interleaving hint (`interleave.count = 1/2/4`): change how many independent vector chains run per iteration.", "leave_alone": "No loop hint would help this loop; leave it as the baseline has it.", "unroll": "An unrolling hint (`unroll.count = 2/4/8` or `unroll.disable`): change how many copies of the body run per iteration, overriding the unroller's own decision.", "vectorize": "A vectorisation width hint (`vectorize.width = 2/4/8/16`): force the number of lanes per vector iteration, and enable vectorisation."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. Before choosing a value, choose a kind: which of these families of hint, if any, has a mechanism that could make this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"interleave": "An interleaving hint (`interleave.count = 1/2/4`): change how many independent vector chains run per iteration.", "leave_alone": "No loop hint would help this loop; leave it as the baseline has it.", "unroll": "An unrolling hint (`unroll.count = 2/4/8` or `unroll.disable`): change how many copies of the body run per iteration, overriding the unroller's own decision.", "vectorize": "A vectorisation width hint (`vectorize.width = 2/4/8/16`): force the number of lanes per vector iteration, and enable vectorisation."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. Before choosing a value, choose a kind: which of these families of hint, if any, has a mechanism that could make this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"interleave": "An interleaving hint (`interleave.count = 1/2/4`): change how many independent vector chains run per iteration.", "leave_alone": "No loop hint would help this loop; leave it as the baseline has it.", "unroll": "An unrolling hint (`unroll.count = 2/4/8` or `unroll.disable`): change how many copies of the body run per iteration, overriding the unroller's own decision.", "vectorize": "A vectorisation width hint (`vectorize.width = 2/4/8/16`): force the number of lanes per vector iteration, and enable vectorisation."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. Before choosing a value, choose a kind: which of these families of hint, if any, has a mechanism that could make this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"interleave": "An interleaving hint (`interleave.count = 1/2/4`): change how many independent vector chains run per iteration.", "leave_alone": "No loop hint would help this loop; leave it as the baseline has it.", "unroll": "An unrolling hint (`unroll.count = 2/4/8` or `unroll.disable`): change how many copies of the body run per iteration, overriding the unroller's own decision.", "vectorize": "A vectorisation width hint (`vectorize.width = 2/4/8/16`): force the number of lanes per vector iteration, and enable vectorisation."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. Before choosing a value, choose a kind: which of these families of hint, if any, has a mechanism that could make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V12, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <&alloc::string::String as core::fmt::Display>::fmt |\n  | that function's perf share (all six workloads) | 1.10% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 5.6% |\n  | average trip count (PGO) | 32.0 |\n  | loop header execution count (PGO) | 722,491,314 |\n  | body size | 36 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 2 |\n  | loops this site key names | 1 (the key names exactly one loop) |\n  | hotness (header count x body instructions) | 2.60e10 --- the largest hotness of any loop_in_mark key in this program |\n  | inline chain from the marked function down to the loop | Formatter::pad -> Chars::count -> do_count_chars -> <slice::Iter<[u8;4]>>::next |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit with complex writes to memory; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) + unroller advises against (contains a call/invoke) + cost model says not beneficial |\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 25.8 |\n  | loop header execution count (PGO) | 144,524,134 |\n  | body size | 141 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 4 (one hint on this key lands on all four loops; their hotness sums to 3.24e10) |\n  | hotness (header count x body instructions) | 2.04e10 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> BufWriter<StdoutLock>::write_fmt -> default_write_fmt -> core::fmt::write |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit; loop induction variable could not be identified; could not determine number of loop iterations) + unroller advises against (contains a call/invoke) |\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <jaq_std::base_run<..>::{closure#7} as FnOnce<..>>::call_once |\n  | that function's perf share (all six workloads) | 1.73% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 4.1% |\n  | its share of the readwrite case | 0.0% |\n  | average trip count (PGO) | 221.7 |\n  | loop header execution count (PGO) | 177,080,637 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.30e9 |\n  | inline chain from the marked function down to the loop | base_run{closure#7} -> map_utf8_str -> ByteSlice::to_ascii_lowercase -> <slice::Iter<u8>>::next |\n  | what the baseline remarks on this loop's line say | LEGAL and already vectorized: `vectorized loop (vectorization width: 4, interleaved count: 2)` and `(width 4, interleaved count: 4)` at this line, plus `completely unrolled`; cost model reports vectorisation and interleaving as not beneficial, and one `runtime pointer checks needed`. Remarks are attributed by source line only, and several loops share macros.rs:279 |\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::read::parse |\n  | that function's perf share (all six workloads) | 29.29% |\n  | its share of the objsearch case | 33.9% |\n  | its share of the strproc case | 26.0% |\n  | its share of the readwrite case | 27.5% |\n  | average trip count (PGO) | 13.9 |\n  | loop header execution count (PGO) | 207,748,447 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.89e9 |\n  | inline chain from the marked function down to the loop | read::parse -> parse_string -> str_fold -> write_until -> Copied<Iter<u8>>::position -> position::check |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (loop contains an unsupported switch; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) |\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 1.0 |\n  | loop header execution count (PGO) | 5,591,880 |\n  | body size | 200 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 1.12e9 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> <slice::Iter<..>>::next |\n  | what the baseline remarks on this loop's line say | not recorded for this site (it is outside the frozen site set, so no remark attribution was run for it) |\n"}, "sha": "ee989a84753b7fae", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": 1, "tag": "V12-L1-L2-L3-L4-L5-stage1", "variant": "V12"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one site of this program. The family of hint to use there has already been decided: inlining. Which value inside that family should the build use?", "type": "choice"}, "q1": {"criteria": {"inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one site of this program. The family of hint to use there has already been decided: inlining. Which value inside that family should the build use?", "type": "choice"}, "q2": {"criteria": {"inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one site of this program. The family of hint to use there has already been decided: inlining. Which value inside that family should the build use?", "type": "choice"}, "q3": {"criteria": {"inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one site of this program. The family of hint to use there has already been decided: inlining. Which value inside that family should the build use?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V12, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 1.73% |\n  | perf reach share (this function anywhere in the inline frame stack) | 1.73% |\n  | share of the objsearch case | 0.0% |\n  | share of the strproc case | 4.1% |\n  | share of the readwrite case | 0.0% |\n  | size and shape after LTO | 251 LLVM insts (432 machine insts, 98 of them vector, 14 machine backedges), 1 monomorphization, 1 loop site inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | already vectorized (width 4, interleave 2 and 4) plus `completely unrolled` at macros.rs:279; cost model says vectorisation and interleaving are not beneficial |\n"}, "sha": "73f9f4bdb7bae387", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": 2, "tag": "V12-F1-F2-F3-F4-stage2", "variant": "V12"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one site of this program. The family of hint to use there has already been decided: inlining. Which value inside that family should the build use?", "type": "choice"}, "q1": {"criteria": {"inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one site of this program. The family of hint to use there has already been decided: inlining. Which value inside that family should the build use?", "type": "choice"}, "q2": {"criteria": {"inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one site of this program. The family of hint to use there has already been decided: inlining. Which value inside that family should the build use?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V12, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (3 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n"}, "sha": "a5418feb307af5b8", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3"}, "stage": 2, "tag": "V12-F1-F2-F3-stage2", "variant": "V12"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V13, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## Six hints of this vocabulary, measured on other programs\n\nThese are real measurements from two earlier targets of this project, not\nthis program. They are here as calibration for how large the effects of this\nvocabulary are and which shapes respond.\n\n1. A byte-counting loop (`bytes.iter().filter(|&&b| b == b'\"').count()`),\n   25% of its program, which LLVM had vectorised at width 4 with interleave\n   4. Forcing `vectorize.width = 32` made it **2.2x slower**: the forced\n   width was materialised as many narrow operations plus a long remainder.\n   Forcing width 16 on the same loop was also slower than the default. A\n   width far above what the element type needs in one register is not a\n   bigger version of the right answer.\n2. A floating-point dot product. A width hint on it changed the program's\n   **output**, because widening a reduction reorders the additions. The build\n   in this study pins `-hints-allow-reordering=false` for that reason, and a\n   non-reassociable reduction is therefore not widened at all --- the hint is\n   accepted and does nothing.\n3. An early-exit search loop (scan until a byte matches). Every width and\n   interleave hint left the machine code bit-identical: the compiler had\n   already reported the loop as not vectorizable, and metadata does not make\n   an illegal transformation legal.\n4. On a compression library, a whole-build sweep of 30 configurations found\n   nothing above the 3% floor from loop hints alone; the largest single\n   effect in the sweep, about +1.6%, came from **capping** unrolling rather\n   than increasing it.\n5. On a JSON processor, a sweep of 33 whole-build configurations produced\n   +2.1% from an SLP-vectorizer threshold, and the next largest effect was\n   from function **alignment** --- as a build-wide setting, applied to every\n   function, not as one function's entry.\n6. Across four programs already built with `-Copt-level=3`, fat LTO,\n   `target-cpu=native` and PGO, the total room found for loop hints was\n   between 0% and 2%. The prior that a given hint at a given site changes the\n   whole program by more than 3% is low, and the useful question is which\n   site has a mechanism at all.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 1.73% |\n  | perf reach share (this function anywhere in the inline frame stack) | 1.73% |\n  | share of the objsearch case | 0.0% |\n  | share of the strproc case | 4.1% |\n  | share of the readwrite case | 0.0% |\n  | size and shape after LTO | 251 LLVM insts (432 machine insts, 98 of them vector, 14 machine backedges), 1 monomorphization, 1 loop site inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | already vectorized (width 4, interleave 2 and 4) plus `completely unrolled` at macros.rs:279; cost model says vectorisation and interleaving are not beneficial |\n"}, "sha": "c0a5c32bbe6cfea2", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V13-F1-F2-F3-F4", "variant": "V13"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V13, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## Six hints of this vocabulary, measured on other programs\n\nThese are real measurements from two earlier targets of this project, not\nthis program. They are here as calibration for how large the effects of this\nvocabulary are and which shapes respond.\n\n1. A byte-counting loop (`bytes.iter().filter(|&&b| b == b'\"').count()`),\n   25% of its program, which LLVM had vectorised at width 4 with interleave\n   4. Forcing `vectorize.width = 32` made it **2.2x slower**: the forced\n   width was materialised as many narrow operations plus a long remainder.\n   Forcing width 16 on the same loop was also slower than the default. A\n   width far above what the element type needs in one register is not a\n   bigger version of the right answer.\n2. A floating-point dot product. A width hint on it changed the program's\n   **output**, because widening a reduction reorders the additions. The build\n   in this study pins `-hints-allow-reordering=false` for that reason, and a\n   non-reassociable reduction is therefore not widened at all --- the hint is\n   accepted and does nothing.\n3. An early-exit search loop (scan until a byte matches). Every width and\n   interleave hint left the machine code bit-identical: the compiler had\n   already reported the loop as not vectorizable, and metadata does not make\n   an illegal transformation legal.\n4. On a compression library, a whole-build sweep of 30 configurations found\n   nothing above the 3% floor from loop hints alone; the largest single\n   effect in the sweep, about +1.6%, came from **capping** unrolling rather\n   than increasing it.\n5. On a JSON processor, a sweep of 33 whole-build configurations produced\n   +2.1% from an SLP-vectorizer threshold, and the next largest effect was\n   from function **alignment** --- as a build-wide setting, applied to every\n   function, not as one function's entry.\n6. Across four programs already built with `-Copt-level=3`, fat LTO,\n   `target-cpu=native` and PGO, the total room found for loop hints was\n   between 0% and 2%. The prior that a given hint at a given site changes the\n   whole program by more than 3% is low, and the useful question is which\n   site has a mechanism at all.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <&alloc::string::String as core::fmt::Display>::fmt |\n  | that function's perf share (all six workloads) | 1.10% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 5.6% |\n  | average trip count (PGO) | 32.0 |\n  | loop header execution count (PGO) | 722,491,314 |\n  | body size | 36 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 2 |\n  | loops this site key names | 1 (the key names exactly one loop) |\n  | hotness (header count x body instructions) | 2.60e10 --- the largest hotness of any loop_in_mark key in this program |\n  | inline chain from the marked function down to the loop | Formatter::pad -> Chars::count -> do_count_chars -> <slice::Iter<[u8;4]>>::next |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit with complex writes to memory; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) + unroller advises against (contains a call/invoke) + cost model says not beneficial |\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 25.8 |\n  | loop header execution count (PGO) | 144,524,134 |\n  | body size | 141 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 4 (one hint on this key lands on all four loops; their hotness sums to 3.24e10) |\n  | hotness (header count x body instructions) | 2.04e10 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> BufWriter<StdoutLock>::write_fmt -> default_write_fmt -> core::fmt::write |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit; loop induction variable could not be identified; could not determine number of loop iterations) + unroller advises against (contains a call/invoke) |\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <jaq_std::base_run<..>::{closure#7} as FnOnce<..>>::call_once |\n  | that function's perf share (all six workloads) | 1.73% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 4.1% |\n  | its share of the readwrite case | 0.0% |\n  | average trip count (PGO) | 221.7 |\n  | loop header execution count (PGO) | 177,080,637 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.30e9 |\n  | inline chain from the marked function down to the loop | base_run{closure#7} -> map_utf8_str -> ByteSlice::to_ascii_lowercase -> <slice::Iter<u8>>::next |\n  | what the baseline remarks on this loop's line say | LEGAL and already vectorized: `vectorized loop (vectorization width: 4, interleaved count: 2)` and `(width 4, interleaved count: 4)` at this line, plus `completely unrolled`; cost model reports vectorisation and interleaving as not beneficial, and one `runtime pointer checks needed`. Remarks are attributed by source line only, and several loops share macros.rs:279 |\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::read::parse |\n  | that function's perf share (all six workloads) | 29.29% |\n  | its share of the objsearch case | 33.9% |\n  | its share of the strproc case | 26.0% |\n  | its share of the readwrite case | 27.5% |\n  | average trip count (PGO) | 13.9 |\n  | loop header execution count (PGO) | 207,748,447 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.89e9 |\n  | inline chain from the marked function down to the loop | read::parse -> parse_string -> str_fold -> write_until -> Copied<Iter<u8>>::position -> position::check |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (loop contains an unsupported switch; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) |\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 1.0 |\n  | loop header execution count (PGO) | 5,591,880 |\n  | body size | 200 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 1.12e9 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> <slice::Iter<..>>::next |\n  | what the baseline remarks on this loop's line say | not recorded for this site (it is outside the frozen site set, so no remark attribution was run for it) |\n"}, "sha": "aa7e2ca41e0cb675", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V13-L1-L2-L3-L4-L5", "variant": "V13"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Change nothing about this function's attributes. This is the 'no change' option: the build keeps whatever LLVM's own cost model decides, exactly as in the baseline.", "align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. Which single hint from the list is most likely to make this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V14, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## How a careful reader would work through a site like these\n\nThis section is a method, not an answer. It states no conclusion about any\nsite in this request.\n\nStart from what the compiler already reported about the region, because it\ndecides which candidates can do anything at all. If the report says the loop\ncould not be vectorized --- an early exit, an unsupported switch, a value\nthat is not a recognised reduction escaping the loop, an induction variable\nor trip count that could not be identified --- then every `vectorize.width`\nand `interleave.count` candidate is inert, and the only live candidates are\nthe unroll ones, because unroll metadata does override the unroller's own\nadvice. If the report instead shows the loop *was* vectorized and at which\nwidth, then the question becomes whether that width uses the machine's vector\nregister for the element type in play, and a loop whose reported width leaves\nmost of the register idle is the rare case where forcing a wider one has a\nmechanism.\n\nThen read the trip count against the body. Unrolling and widening both buy\nfewer iterations of loop control and pay for it with a remainder loop and\ncode size. A high trip count over a tiny body is where that trade is\nfavourable; a trip count of one or two makes every such candidate pure cost,\nwhatever the loop's share of the profile. A body containing a call is a third\ncase: the call dominates, so the loop-control saving is invisible, and this\nis also why the unroller declines on its own.\n\nFor a function attribute, read the size and the number of monomorphizations\nagainst the attributes the function already carries. An `inline` candidate is\na no-op on a function that already has the hint, and it is the wrong\ndirection on a body large enough that no threshold would admit it anyway.\n`inline(never)` has a mechanism only where a large body is being replicated\ninto callers and instruction cache is the constraint. `cold` is a statement\nthat the function is off the hot path, so on anything holding a real share of\nthe cycles it is a pessimisation regardless of anything else. Alignment\ncandidates have no site-specific mechanism to reason from.\n\nFinally weigh the site's share. A hint has to move the whole program, so the\nmost a site can contribute is bounded by its share of the cycles, and a\nperfect improvement inside a 1% site cannot reach a 3% whole-program\nthreshold. The honest answer at a site where no candidate has a mechanism is\nto leave it alone, and a reader who never answers that way is not reading.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 1.73% |\n  | perf reach share (this function anywhere in the inline frame stack) | 1.73% |\n  | share of the objsearch case | 0.0% |\n  | share of the strproc case | 4.1% |\n  | share of the readwrite case | 0.0% |\n  | size and shape after LTO | 251 LLVM insts (432 machine insts, 98 of them vector, 14 machine backedges), 1 monomorphization, 1 loop site inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | already vectorized (width 4, interleave 2 and 4) plus `completely unrolled` at macros.rs:279; cost model says vectorisation and interleaving are not beneficial |\n"}, "sha": "aa86725a70e63fd6", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V14-F1-F2-F3-F4", "variant": "V14"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"KEEP_DEFAULT": "Attach no metadata to this loop. This is the 'no change' option: LLVM's vectoriser and unroller decide with their own cost model, exactly as in the baseline.", "interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. Which single hint from the list is most likely to make this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V14, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nOne of the options at every site is \"no change\", which reproduces the\nbaseline at that site. It is one option among the others, neither preferred\nnor discouraged. The measurement is end-to-end wall time over the case set\nbelow; the noise floor of this machine is about 1% aggregated. Hints that\nchange the program's output are rejected regardless of speed.\n\n## How a careful reader would work through a site like these\n\nThis section is a method, not an answer. It states no conclusion about any\nsite in this request.\n\nStart from what the compiler already reported about the region, because it\ndecides which candidates can do anything at all. If the report says the loop\ncould not be vectorized --- an early exit, an unsupported switch, a value\nthat is not a recognised reduction escaping the loop, an induction variable\nor trip count that could not be identified --- then every `vectorize.width`\nand `interleave.count` candidate is inert, and the only live candidates are\nthe unroll ones, because unroll metadata does override the unroller's own\nadvice. If the report instead shows the loop *was* vectorized and at which\nwidth, then the question becomes whether that width uses the machine's vector\nregister for the element type in play, and a loop whose reported width leaves\nmost of the register idle is the rare case where forcing a wider one has a\nmechanism.\n\nThen read the trip count against the body. Unrolling and widening both buy\nfewer iterations of loop control and pay for it with a remainder loop and\ncode size. A high trip count over a tiny body is where that trade is\nfavourable; a trip count of one or two makes every such candidate pure cost,\nwhatever the loop's share of the profile. A body containing a call is a third\ncase: the call dominates, so the loop-control saving is invisible, and this\nis also why the unroller declines on its own.\n\nFor a function attribute, read the size and the number of monomorphizations\nagainst the attributes the function already carries. An `inline` candidate is\na no-op on a function that already has the hint, and it is the wrong\ndirection on a body large enough that no threshold would admit it anyway.\n`inline(never)` has a mechanism only where a large body is being replicated\ninto callers and instruction cache is the constraint. `cold` is a statement\nthat the function is off the hot path, so on anything holding a real share of\nthe cycles it is a pessimisation regardless of anything else. Alignment\ncandidates have no site-specific mechanism to reason from.\n\nFinally weigh the site's share. A hint has to move the whole program, so the\nmost a site can contribute is bounded by its share of the cycles, and a\nperfect improvement inside a 1% site cannot reach a 3% whole-program\nthreshold. The honest answer at a site where no candidate has a mechanism is\nto leave it alone, and a reader who never answers that way is not reading.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <&alloc::string::String as core::fmt::Display>::fmt |\n  | that function's perf share (all six workloads) | 1.10% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 5.6% |\n  | average trip count (PGO) | 32.0 |\n  | loop header execution count (PGO) | 722,491,314 |\n  | body size | 36 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 2 |\n  | loops this site key names | 1 (the key names exactly one loop) |\n  | hotness (header count x body instructions) | 2.60e10 --- the largest hotness of any loop_in_mark key in this program |\n  | inline chain from the marked function down to the loop | Formatter::pad -> Chars::count -> do_count_chars -> <slice::Iter<[u8;4]>>::next |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit with complex writes to memory; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) + unroller advises against (contains a call/invoke) + cost model says not beneficial |\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 25.8 |\n  | loop header execution count (PGO) | 144,524,134 |\n  | body size | 141 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 4 (one hint on this key lands on all four loops; their hotness sums to 3.24e10) |\n  | hotness (header count x body instructions) | 2.04e10 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> BufWriter<StdoutLock>::write_fmt -> default_write_fmt -> core::fmt::write |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit; loop induction variable could not be identified; could not determine number of loop iterations) + unroller advises against (contains a call/invoke) |\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <jaq_std::base_run<..>::{closure#7} as FnOnce<..>>::call_once |\n  | that function's perf share (all six workloads) | 1.73% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 4.1% |\n  | its share of the readwrite case | 0.0% |\n  | average trip count (PGO) | 221.7 |\n  | loop header execution count (PGO) | 177,080,637 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.30e9 |\n  | inline chain from the marked function down to the loop | base_run{closure#7} -> map_utf8_str -> ByteSlice::to_ascii_lowercase -> <slice::Iter<u8>>::next |\n  | what the baseline remarks on this loop's line say | LEGAL and already vectorized: `vectorized loop (vectorization width: 4, interleaved count: 2)` and `(width 4, interleaved count: 4)` at this line, plus `completely unrolled`; cost model reports vectorisation and interleaving as not beneficial, and one `runtime pointer checks needed`. Remarks are attributed by source line only, and several loops share macros.rs:279 |\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::read::parse |\n  | that function's perf share (all six workloads) | 29.29% |\n  | its share of the objsearch case | 33.9% |\n  | its share of the strproc case | 26.0% |\n  | its share of the readwrite case | 27.5% |\n  | average trip count (PGO) | 13.9 |\n  | loop header execution count (PGO) | 207,748,447 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.89e9 |\n  | inline chain from the marked function down to the loop | read::parse -> parse_string -> str_fold -> write_until -> Copied<Iter<u8>>::position -> position::check |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (loop contains an unsupported switch; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) |\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 1.0 |\n  | loop header execution count (PGO) | 5,591,880 |\n  | body size | 200 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 1.12e9 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> <slice::Iter<..>>::next |\n  | what the baseline remarks on this loop's line say | not recorded for this site (it is outside the frozen site set, so no remark attribution was run for it) |\n"}, "sha": "9248249cd78b11ce", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V14-L1-L2-L3-L4-L5", "variant": "V14"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V15, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nAt each site in this request the build **will** apply one of the listed\nhints. The question is which one. The measurement is end-to-end wall time\nover the case set below; the noise floor of this machine is about 1%\naggregated. Hints that change the program's output are rejected regardless of\nspeed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 1.73% |\n  | perf reach share (this function anywhere in the inline frame stack) | 1.73% |\n  | share of the objsearch case | 0.0% |\n  | share of the strproc case | 4.1% |\n  | share of the readwrite case | 0.0% |\n  | size and shape after LTO | 251 LLVM insts (432 machine insts, 98 of them vector, 14 machine backedges), 1 monomorphization, 1 loop site inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | already vectorized (width 4, interleave 2 and 4) plus `completely unrolled` at macros.rs:279; cost model says vectorisation and interleaving are not beneficial |\n"}, "sha": "4ee4b6b6487ffd23", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V15-F1-F2-F3-F4", "variant": "V15"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V15, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nAt each site in this request the build **will** apply one of the listed\nhints. The question is which one. The measurement is end-to-end wall time\nover the case set below; the noise floor of this machine is about 1%\naggregated. Hints that change the program's output are rejected regardless of\nspeed.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <&alloc::string::String as core::fmt::Display>::fmt |\n  | that function's perf share (all six workloads) | 1.10% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 5.6% |\n  | average trip count (PGO) | 32.0 |\n  | loop header execution count (PGO) | 722,491,314 |\n  | body size | 36 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 2 |\n  | loops this site key names | 1 (the key names exactly one loop) |\n  | hotness (header count x body instructions) | 2.60e10 --- the largest hotness of any loop_in_mark key in this program |\n  | inline chain from the marked function down to the loop | Formatter::pad -> Chars::count -> do_count_chars -> <slice::Iter<[u8;4]>>::next |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit with complex writes to memory; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) + unroller advises against (contains a call/invoke) + cost model says not beneficial |\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 25.8 |\n  | loop header execution count (PGO) | 144,524,134 |\n  | body size | 141 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 4 (one hint on this key lands on all four loops; their hotness sums to 3.24e10) |\n  | hotness (header count x body instructions) | 2.04e10 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> BufWriter<StdoutLock>::write_fmt -> default_write_fmt -> core::fmt::write |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit; loop induction variable could not be identified; could not determine number of loop iterations) + unroller advises against (contains a call/invoke) |\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <jaq_std::base_run<..>::{closure#7} as FnOnce<..>>::call_once |\n  | that function's perf share (all six workloads) | 1.73% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 4.1% |\n  | its share of the readwrite case | 0.0% |\n  | average trip count (PGO) | 221.7 |\n  | loop header execution count (PGO) | 177,080,637 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.30e9 |\n  | inline chain from the marked function down to the loop | base_run{closure#7} -> map_utf8_str -> ByteSlice::to_ascii_lowercase -> <slice::Iter<u8>>::next |\n  | what the baseline remarks on this loop's line say | LEGAL and already vectorized: `vectorized loop (vectorization width: 4, interleaved count: 2)` and `(width 4, interleaved count: 4)` at this line, plus `completely unrolled`; cost model reports vectorisation and interleaving as not beneficial, and one `runtime pointer checks needed`. Remarks are attributed by source line only, and several loops share macros.rs:279 |\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::read::parse |\n  | that function's perf share (all six workloads) | 29.29% |\n  | its share of the objsearch case | 33.9% |\n  | its share of the strproc case | 26.0% |\n  | its share of the readwrite case | 27.5% |\n  | average trip count (PGO) | 13.9 |\n  | loop header execution count (PGO) | 207,748,447 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.89e9 |\n  | inline chain from the marked function down to the loop | read::parse -> parse_string -> str_fold -> write_until -> Copied<Iter<u8>>::position -> position::check |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (loop contains an unsupported switch; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) |\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 1.0 |\n  | loop header execution count (PGO) | 5,591,880 |\n  | body size | 200 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 1.12e9 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> <slice::Iter<..>>::next |\n  | what the baseline remarks on this loop's line say | not recorded for this site (it is outside the frozen site set, so no remark attribution was run for it) |\n"}, "sha": "8dee1d65e62f5ef1", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V15-L1-L2-L3-L4-L5", "variant": "V15"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q0` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}, "q1": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q1` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}, "q2": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q2` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}, "q3": {"criteria": {"align_16": "Set the function's alignment to 16 bytes (the SPEC vocabulary's `align=16`). This is the usual default on x86-64, so it mostly means 'pin the current alignment'.", "align_32": "Set the function's alignment to 32 bytes (`align=32`). The entry point starts on a 32-byte boundary, which changes how the function's first instructions and its first loop fall into the 32-byte instruction-fetch windows and the uop cache.", "align_64": "Set the function's alignment to 64 bytes (`align=64`). The function starts on a cache line. Same mechanism as align=32, one step coarser, and it wastes up to 63 bytes of padding per function.", "cold": "Add the `cold` attribute. It tells LLVM this function is rarely executed: callers place calls to it out of line, it is optimised for size rather than speed, and it is never inlined. Correct only if the function really is off the hot path; on a hot function it is a pessimisation.", "inline": "Add the `inlinehint` attribute (the SPEC vocabulary's `inline`). It raises the inliner's threshold for this function, so it is inlined into callers it would otherwise be too large for. Its own loops then become loops of the callers, and specialisation on the call site becomes possible; code size and I-cache pressure grow.", "inline_never": "Add the `noinline` attribute (the SPEC vocabulary's `inline(never)`). The function stays a single out-of-line copy. Call overhead is paid at every call site, and in exchange the callers stay small and the function's own body is optimised once for all of them."}, "instructions": "Section `q3` of the state describes one marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this function faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V16, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nAt each site in this request the build **will** apply one of the listed\nhints. The question is which one. The measurement is end-to-end wall time\nover the case set below; the noise floor of this machine is about 1%\naggregated. Hints that change the program's output are rejected regardless of\nspeed.\n\n## What each hint in the list does, in general\n\nThis section is about the hints themselves. It says nothing about the sites\nin this request.\n\n`inline` (`inlinehint`)\n  Raises the inliner's threshold for one function, so callers that were just\n  over the limit now paste its body in. It pays on a small, hot function\n  called from few places, where the caller then specialises on constants it\n  passes. It backfires on a large body, on a function called from many\n  places (every copy costs instruction cache), and it is a no-op when the\n  function already carries the attribute or is already inlined everywhere.\n\n`inline(never)` (`noinline`)\n  Forces one out-of-line copy. It pays when a large body was being pasted\n  into many callers and the instruction cache, not the call overhead, is the\n  cost --- typically a big dispatch or a recursive body with many\n  monomorphizations. It costs a call and the lost specialisation at every\n  call site, so it hurts on a small hot leaf.\n\n`cold`\n  Tells LLVM the function is rarely executed: calls to it move out of line,\n  it is optimised for size, and it is never inlined. It is correct only for\n  error and fallback paths. On anything that carries a real share of the\n  cycles it is a straight pessimisation.\n\n`align=16 / 32 / 64`\n  Moves the function's entry to that boundary, which changes how its first\n  instructions and its first loop fall into the 32-byte fetch windows and the\n  uop cache. 16 is already the x86-64 default, so it mostly pins what is\n  there. Larger values can help a function whose hot loop sits near its\n  entry, they waste padding, and their effect is close to unpredictable ---\n  when alignment has been measured to matter it was usually as a whole-build\n  setting rather than one function's entry.\n\n`unroll.count = 2 / 4 / 8`\n  Overrides the unroller, including its own advice not to unroll. It pays on\n  a short body with a high trip count, where loop control and the dependence\n  chain between iterations are a real fraction of the work. It costs code\n  size and a longer remainder loop, so it hurts when the trip count is low,\n  when the body is already large, or when the body contains a call --- the\n  call dominates whatever the loop control cost. Note that unrolling runs\n  after vectorisation, so on a loop LLVM vectorises this unrolls the vector\n  loop.\n\n`unroll.disable`\n  Forbids unrolling. It pays when the caller's instruction footprint is the\n  problem, or when the loop is short and its trip count unpredictable.\n\n`vectorize.width = 2 / 4 / 8 / 16`\n  Forces the number of lanes per vector iteration and enables vectorisation.\n  The right width is a property of the element type and the machine's vector\n  register: lanes x element size should reach the register width, and going\n  past it makes LLVM emit several registers per iteration. It pays when the\n  loop is long, straight-line, has no calls, and LLVM chose a width below\n  what the element type allows. It is **not a permission slip**: when the\n  compiler reports the loop as illegal to vectorize --- an early exit, an\n  unsupported switch, a value that is not a recognised reduction escaping the\n  loop, an undeterminable trip count --- the hint is dropped and the build\n  is unchanged. It hurts when the trip count is too low to amortise the\n  remainder loop, and a width far above the register width has been measured\n  to produce code several times slower than the default.\n\n`interleave.count = 1 / 2 / 4`\n  How many independent vector bodies run per iteration. More chains hide the\n  latency of a dependent reduction at the cost of registers and a longer\n  remainder; `1` shortens the loop when the default interleaving was too\n  aggressive for the trip count.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  This is round 1: nothing has been measured yet. The baseline is the build in which every site is KEEP_DEFAULT.\n\n## The sites in this request (4 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::read::parse\n               18 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  29.29% of the program's user cycles, 29.29% counting the code inlined into it\nsize after LTO 7660 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nwhat LLVM said about this region in the baseline build:\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsJiC9gTn727_8jaq_fmts' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:119: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=45, threshold=45)\n  read.rs:121: '_RNvXNtCs6u2SwoadH4g_8hifijson4readNtB4_10SliceLexerNtB2_4Read12strip_prefix' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read5parseNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=45, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 29.29% |\n  | perf reach share (this function anywhere in the inline frame stack) | 29.29% |\n  | share of the objsearch case | 33.9% |\n  | share of the strproc case | 26.0% |\n  | share of the readwrite case | 27.5% |\n  | size and shape after LTO | 7660 LLVM insts (4426 machine insts, 259 machine backedges; 805 insts are read::parse's own body, the rest is inlined into it), 18 monomorphizations, 49 loop sites inside it |\n  | attributes it carries now | cold or inlinehint (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals only (panic paths cost=never, strip_prefix cost=45 vs threshold=45); no vectorisation remark on the function's own line |\n\n### q1\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_core::compile::TermId>::run\n               62 monomorphizations of it exist in this build; the attribute goes on all of them\nprofile share  8.32% of the program's user cycles, 8.85% counting the code inlined into it\nsize after LTO 10496 LLVM instructions, 0 loop(s) inside it\nattributes now cold | inlinehint | inlinehint,cold  (the distinct sets over all of them)\n\nsource:\ntargets/jaq/src/jaq-core/src/filter.rs:464 (lines 424-504, the site's own line marked `>`)\n  424  }\n  425  \n  426  type IdRunFn<'a, D, T> = fn(&Id, Cv<'a, D, T>) -> ValXs<'a, T, <D as DataT>::V<'a>>;\n  427  \n  428  /// Run function pointer (see [`Id::run`]).\n  429  pub type RunPtr<D> = for<'a> fn(Cv<'a, D>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  430  /// Paths function pointer (see [`Id::paths`]).\n  431  pub type PathsPtr<D> = for<'a> fn(Cvp<'a, D>) -> ValPathXs<'a, <D as DataT>::V<'a>>;\n  432  /// Update function pointer (see [`Id::update`]).\n  433  pub type UpdatePtr<D> =\n  434      for<'a> fn(Cv<'a, D>, BoxUpdate<'a, <D as DataT>::V<'a>>) -> ValXs<'a, <D as DataT>::V<'a>>;\n  435  \n  436  impl<D: DataT> Native<D> {\n  437      /// Create a native filter from a run function.\n  438      ///\n  439      /// A filter created this way initially supports neither paths nor updates.\n  440      /// For that, use [`Self::with_paths`] and [`Self::with_update`].\n  441      pub const fn new(run: RunPtr<D>) -> Self {\n  442          Self {\n  443              run,\n  444              paths: |cv| box_once(Err(Exn::from(Error::path_expr(cv.1 .0)))),\n  445              update: |cv, _| box_once(Err(Exn::from(Error::path_expr(cv.1)))),\n  446          }\n  447      }\n  448  \n  449      /// Specify a paths function (used for `path(...)`).\n  450      pub const fn with_paths(self, paths: PathsPtr<D>) -> Self {\n  451          Self { paths, ..self }\n  452      }\n  453  \n  454      /// Specify an update function (used for `filter |= ...`).\n  455      ///\n  456      /// If an update function is given, then a paths function should be implemented too.\n  457      pub const fn with_update(self, update: UpdatePtr<D>) -> Self {\n  458          Self { update, ..self }\n  459      }\n  460  }\n  461  \n  462  impl Id {\n  463      /// `f.run((c, v))` returns the output of `v | f` in the context `c`.\n  464 >    pub fn run<'a, D: DataT>(&self, cv: Cv<'a, D>) -> ValXs<'a, D::V<'a>> {\n  465          use core::iter::once;\n  466          match &cv.0.lut().terms[self.0] {\n  467              Ast::Id => box_once(Ok(cv.1)),\n  468              Ast::Recurse => recurse_run(cv.1, &|v| v.values()),\n  469              Ast::ToString => box_once(Ok(cv.1.into_string())),\n  470              Ast::Int(n) => box_once(Ok(D::V::from(*n))),\n  471              Ast::Num(x) => box_once(D::V::from_num(x).map_err(Exn::from)),\n  472              Ast::Str(s) => box_once(Ok(D::V::from(s.clone()))),\n  473              Ast::Arr(f) => box_once(f.run(cv).collect()),\n  474              Ast::ObjEmpty => box_once(D::V::from_map([]).map_err(Exn::from)),\n  475              Ast::ObjSingle(k, v) => {\n  476                  Box::new(cartesian(k, v, cv).map(|(k, v)| Ok(D::V::from_map([(k?, v?)])?)))\n  477              }\n  478              Ast::TryCatch(f, c) => try_catch_run(f.run((cv.0.clone(), cv.1)), move |e| {\n  479                  c.run((cv.0.clone(), e.into_val()))\n  480              }),\n  481              Ast::Neg(f) => Box::new(f.run(cv).map(|v| Ok((-v?)?))),\n  482  \n  483              // `l | r`\n  484              Ast::Pipe(l, None, r) => {\n  485                  flat_map_then_with(l.run((cv.0.clone(), cv.1)), cv.0, move |y, ctx| {\n  486                      r.run((ctx, y))\n  487                  })\n  488              }\n  489              // `l as $x | r`, `l as [...] | r`, or `l as {...} | r`\n  490              Ast::Pipe(l, Some(pat), r) => pipe(l, cv, move |cv, y| {\n  491                  bind_run(pat, r, cv, y, |f, cv| f.run(cv))\n  492              }),\n  493              Ast::Comma(l, r) => Box::new(l.run(cv.clone()).chain(lazy(|| r.run(cv)))),\n  494              Ast::Alt(l, r) => {\n  495                  let mut l = l\n  496                      .run(cv.clone())\n  497                      .filter(|v| v.as_ref().map_or(true, ValT::as_bool));\n  498                  match l.next() {\n  499                      Some(head) => Box::new(once(head).chain(l)),\n  500                      None => r.run(cv),\n  501                  }\n  502              }\n  503              Ast::Ite(if_, then_, else_) => pipe(if_, cv, move |cv, v| {\n  504                  if v.as_bool() { then_ } else { else_ }.run(cv)\n\nwhat LLVM said about this region in the baseline build:\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:464: '_RNvNtCs2ubz5g4KsBC_4core9panicking16panic_in_cleanup' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because it should never be inlined (cost=never): noinline function attribute\n  filter.rs:466: Cannot SLP vectorize list: vectorization was impossible with available vectorization factors\n  filter.rs:467: '_RINvNtCs3t0tOJGAbYW_8jaq_core8box_iter8box_onceINtNtCs2ubz5g4KsBC_4core6result6ResultNtCslKRKzoMO8pS_8jaq_json3ValINtNtB4_3exn3ExnB1l_EEECstofKklK7j5_7jaq_all' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=230, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0EB4p_' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindEB16_' because too costly to inline (cost=390, threshold=45)\n  filter.rs:468: '_RNvXs_CslKRKzoMO8pS_8jaq_jsonNtB4_3ValNtNtCs3t0tOJGAbYW_8jaq_core3val4ValT6values' not inlined into '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' because too costly to inline (cost=1055, threshold=45)\n  filter.rs:468: '_RINvNtCs3t0tOJGAbYW_8jaq_core6filter11recurse_runNtCslKRKzoMO8pS_8jaq_json3ValBL_INtNtCsecSPvMcbw1R_5alloc5boxed3BoxDNtNtNtNtCs2ubz5g4KsBC_4core4iter6traits8iterator8Iteratorp4ItemINtNtB1Z_6result6ResultBL_INtNtB4_3exn5ErrorBL_EEEL_ENCINvMs5_B2_NtNtB4_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindE0ECsayvNaZMR3fG_3jaq' not inlined into '_RINvMs5_NtCs3t0tOJGAbYW_8jaq_core6filterNtNtB8_7compile6TermId3runNtNtCstofKklK7j5_7jaq_all4data8DataKindECsayvNaZMR3fG_3jaq' because too costly to inline (cost=390, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 8.32% |\n  | perf reach share (this function anywhere in the inline frame stack) | 8.85% |\n  | share of the objsearch case | 6.8% |\n  | share of the strproc case | 13.2% |\n  | share of the readwrite case | 0.8% |\n  | size and shape after LTO | 10496 LLVM insts (6562 machine insts, 236 machine backedges; 1322 insts are its own body), 62 monomorphizations, 15 loop sites inside it |\n  | attributes it carries now | cold, or inlinehint, or inlinehint+cold (the distinct sets over the copies) |\n  | what the baseline remarks on this function's lines are about | inlining refusals; one `Cannot SLP vectorize list` at filter.rs:466 |\n\n### q2\n\nsite kind      one marked function (function attribute)\nfunction       jaq_json::write::write\n               (7 closure(s) defined inside it keep their own attributes)\nprofile share  4.48% of the program's user cycles\nsize after LTO 5467 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  write.rs:303: failed to move load with loop-invariant address because the loop may invalidate its value\n  write.rs:303: load of type i64 not eliminated\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_' with (cost=20, threshold=250) at callsite _RNvYNtNtNtNtCs1mKxMHUCrv1_3std3sys5stdio4unix6StderrNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredBa_:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECs1mKxMHUCrv1_3std' inlined into '_RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored' with (cost=20, threshold=250) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered14linewritershim14LineWriterShimNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1j_:12:13 @ _RNvXs0_NtNtNtCsecSPvMcbw1R_5alloc2io8buffered10linewriterINtB5_10LineWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdoutRawENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredB1h_:1:46 @ _RNvXsi_NtNtCs1mKxMHUCrv1_3std2io5stdioNtB5_10StdoutLockNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectored:1:33;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:290: '_RINvNtCs2ubz5g4KsBC_4core3ptr9drop_glueNtNtNtB4_2io5error5ErrorECsayvNaZMR3fG_3jaq' inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' with (cost=20, threshold=45) at callsite _RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq:12:13;\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYINtNtNtNtCsecSPvMcbw1R_5alloc2io8buffered9bufwriter9BufWriterNtNtNtCs1mKxMHUCrv1_3std2io5stdio10StdoutLockENtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n  write.rs:288: '_RNvMs1_NtNtCs2ubz5g4KsBC_4core2io5errorNtB5_5Error14is_interrupted' not inlined into '_RNvYNtNtCs1mKxMHUCrv1_3std2fs4FileNtNtNtCs2ubz5g4KsBC_4core2io5write5Write18write_all_vectoredCsayvNaZMR3fG_3jaq' because too costly to inline (cost=60, threshold=45)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 4.48% |\n  | perf reach share (this function anywhere in the inline frame stack) | 4.48% |\n  | share of the objsearch case | 0.1% |\n  | share of the strproc case | 0.0% |\n  | share of the readwrite case | 22.6% |\n  | size and shape after LTO | 5467 LLVM insts (2390 machine insts, 179 machine backedges; 749 insts are its own body), 1 monomorphization plus 7 closures defined inside it, 22 loop sites inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | load not eliminated / loop-invariant load could not be moved at write.rs:303; inlining decisions elsewhere |\n\n### q3\n\nsite kind      one marked function (function attribute)\nfunction       <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nprofile share  1.73% of the program's user cycles\nsize after LTO 251 LLVM instructions, 0 loop(s) inside it\nattributes now inlinehint\n\nsource:\n  (not available: this function's source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this function, as a table\n\n  | field | value |\n  |---|---|\n  | perf self share (all six workloads) | 1.73% |\n  | perf reach share (this function anywhere in the inline frame stack) | 1.73% |\n  | share of the objsearch case | 0.0% |\n  | share of the strproc case | 4.1% |\n  | share of the readwrite case | 0.0% |\n  | size and shape after LTO | 251 LLVM insts (432 machine insts, 98 of them vector, 14 machine backedges), 1 monomorphization, 1 loop site inside it |\n  | attributes it carries now | inlinehint |\n  | what the baseline remarks on this function's lines are about | already vectorized (width 4, interleave 2 and 4) plus `completely unrolled` at macros.rs:279; cost model says vectorisation and interleaving are not beneficial |\n"}, "sha": "56d73236642c081f", "site_map": {"q0": "F1", "q1": "F2", "q2": "F3", "q3": "F4"}, "stage": null, "tag": "V16-F1-F2-F3-F4", "variant": "V16"}
{"request": {"model": "typesafe-ai/jev", "questions": {"q0": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q0` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q1": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q1` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q2": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q2` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q3": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q3` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}, "q4": {"criteria": {"interleave_count_1": "Attach `llvm.loop.interleave.count = 1`: vectorise without interleaving, i.e. one vector body per iteration and a single accumulator chain. Shortens the loop and the remainder when LLVM's default interleaving was too aggressive.", "interleave_count_2": "Attach `llvm.loop.interleave.count = 2`: two independent vector chains per iteration, which hides the latency of a dependent reduction at the cost of more registers.", "interleave_count_4": "Attach `llvm.loop.interleave.count = 4`: four independent vector chains per iteration. The most latency hiding of the three and the most register pressure; on a short trip count the remainder loop eats the gain.", "unroll_count_2": "Attach `llvm.loop.unroll.count = 2`: unroll the loop body twice. Halves the loop-control overhead and gives the scheduler two iterations to interleave. Note that this runs after vectorisation, so on a loop LLVM vectorises it unrolls the *vector* loop.", "unroll_count_4": "Attach `llvm.loop.unroll.count = 4`: unroll the loop body four times. More scheduling freedom and fewer branches than count=2, at four times the body's code size and a longer remainder loop.", "unroll_count_8": "Attach `llvm.loop.unroll.count = 8`: unroll the loop body eight times. Only worth it for a very short body with a high trip count; otherwise the code growth costs more in instruction cache than the saved branches are worth.", "unroll_disable": "Attach `llvm.loop.unroll.disable`: forbid unrolling this loop entirely. Keeps the body small, which helps when the loop is short and unpredictable or when the caller's instruction footprint is the problem.", "vectorize_width_16": "Attach `llvm.loop.vectorize.width = 16` (and `vectorize.enable`): sixteen lanes per vector iteration. Only 8-bit and 16-bit element types fit that in one AVX2 register; for anything wider LLVM emits several registers per iteration, which can pay off on a very long loop and hurts on a short one.", "vectorize_width_2": "Attach `llvm.loop.vectorize.width = 2` (and `vectorize.enable`): force two lanes per vector iteration. A narrow forced width is a way to keep vectorisation but cut the cost of the scalar remainder when the trip count is low. Reassociation of floating-point reductions stays forbidden (the build pins -hints-allow-reordering=false), so a non-reassociable reduction will simply not be widened.", "vectorize_width_4": "Attach `llvm.loop.vectorize.width = 4` (and `vectorize.enable`): four lanes per vector iteration. On this machine (znver3, AVX2, no AVX-512) that is one 256-bit register for 64-bit elements.", "vectorize_width_8": "Attach `llvm.loop.vectorize.width = 8` (and `vectorize.enable`): eight lanes per vector iteration. For 32-bit elements that is one 256-bit register; for 64-bit elements it is two, which LLVM will split."}, "instructions": "Section `q4` of the state describes one loop inside a marked function of this program. We are going to try exactly one hint on it in the next build. Which of these is the most promising for making this loop faster on this workload?", "type": "choice"}}, "state": "# jev-opt prompt study --- prompt-study-v1-2026-09-22, variant V16, vocabulary v1-2026-09-22\n\n\n## What is being decided\n\nA Rust program is compiled with one frozen recipe. The only thing that varies\nbetween builds is a set of optimisation hints attached to named functions and\nto the loops inside them; no source file is ever edited. You are shown one\nsite per question and you choose one hint for it from a fixed list. All the\nquestions in this request are answered independently and they all take effect\nin the same build, which is then measured as a whole.\n\nAt each site in this request the build **will** apply one of the listed\nhints. The question is which one. The measurement is end-to-end wall time\nover the case set below; the noise floor of this machine is about 1%\naggregated. Hints that change the program's output are rejected regardless of\nspeed.\n\n## What each hint in the list does, in general\n\nThis section is about the hints themselves. It says nothing about the sites\nin this request.\n\n`inline` (`inlinehint`)\n  Raises the inliner's threshold for one function, so callers that were just\n  over the limit now paste its body in. It pays on a small, hot function\n  called from few places, where the caller then specialises on constants it\n  passes. It backfires on a large body, on a function called from many\n  places (every copy costs instruction cache), and it is a no-op when the\n  function already carries the attribute or is already inlined everywhere.\n\n`inline(never)` (`noinline`)\n  Forces one out-of-line copy. It pays when a large body was being pasted\n  into many callers and the instruction cache, not the call overhead, is the\n  cost --- typically a big dispatch or a recursive body with many\n  monomorphizations. It costs a call and the lost specialisation at every\n  call site, so it hurts on a small hot leaf.\n\n`cold`\n  Tells LLVM the function is rarely executed: calls to it move out of line,\n  it is optimised for size, and it is never inlined. It is correct only for\n  error and fallback paths. On anything that carries a real share of the\n  cycles it is a straight pessimisation.\n\n`align=16 / 32 / 64`\n  Moves the function's entry to that boundary, which changes how its first\n  instructions and its first loop fall into the 32-byte fetch windows and the\n  uop cache. 16 is already the x86-64 default, so it mostly pins what is\n  there. Larger values can help a function whose hot loop sits near its\n  entry, they waste padding, and their effect is close to unpredictable ---\n  when alignment has been measured to matter it was usually as a whole-build\n  setting rather than one function's entry.\n\n`unroll.count = 2 / 4 / 8`\n  Overrides the unroller, including its own advice not to unroll. It pays on\n  a short body with a high trip count, where loop control and the dependence\n  chain between iterations are a real fraction of the work. It costs code\n  size and a longer remainder loop, so it hurts when the trip count is low,\n  when the body is already large, or when the body contains a call --- the\n  call dominates whatever the loop control cost. Note that unrolling runs\n  after vectorisation, so on a loop LLVM vectorises this unrolls the vector\n  loop.\n\n`unroll.disable`\n  Forbids unrolling. It pays when the caller's instruction footprint is the\n  problem, or when the loop is short and its trip count unpredictable.\n\n`vectorize.width = 2 / 4 / 8 / 16`\n  Forces the number of lanes per vector iteration and enables vectorisation.\n  The right width is a property of the element type and the machine's vector\n  register: lanes x element size should reach the register width, and going\n  past it makes LLVM emit several registers per iteration. It pays when the\n  loop is long, straight-line, has no calls, and LLVM chose a width below\n  what the element type allows. It is **not a permission slip**: when the\n  compiler reports the loop as illegal to vectorize --- an early exit, an\n  unsupported switch, a value that is not a recognised reduction escaping the\n  loop, an undeterminable trip count --- the hint is dropped and the build\n  is unchanged. It hurts when the trip count is too low to amortise the\n  remainder loop, and a width far above the register width has been measured\n  to produce code several times slower than the default.\n\n`interleave.count = 1 / 2 / 4`\n  How many independent vector bodies run per iteration. More chains hide the\n  latency of a dependent reduction at the cost of registers and a longer\n  remainder; `1` shortens the loop when the default interleaving was too\n  aggressive for the trip count.\n\n## The build every arm shares\n\ntarget        jaq\nbinary        jaq\ntoolchain     rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\nmachine       AMD Ryzen 9 5950X (znver3, AVX2, no AVX-512), one physical core\nrecipe        -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n              -Ctarget-cpu=native, PGO (-Cprofile-use, one shared profile),\n              -Cllvm-args=-hints-allow-reordering=false\nmeasurement   3 case(s): objsearch, strproc, readwrite\n              15 interleaved repetitions, shuffled label order, paired\n              bootstrap 95% CI, speed ratio = baseline time / this build's time\n              (greater than 1 is faster)\n\n## The machine this will run on\n\n  cpu model         AMD Ryzen 9 5950X, 16 cores / 32 threads, family 25 model 33\n  microarchitecture Zen 3 (LLVM target-cpu `znver3`)\n  isa               AVX, AVX2, FMA, BMI1, BMI2, AES, SHA, VAES, VPCLMULQDQ,\n                    POPCNT, MOVBE, RDSEED, ADX, CLWB, CLFLUSHOPT, ERMS, FSRM\n  no isa            **no AVX-512 of any kind**, no AMX, no SVE\n  vector width      256 bit, 16 architectural ymm registers\n                    one ymm holds 32 x u8, 16 x u16, 8 x u32/f32, 4 x u64/f64\n                    two 256-bit FP/vector pipes, 2 loads + 1 store per cycle\n  l1 data           32 KiB, 8-way, per core\n  l1 instruction    32 KiB, 8-way, per core\n  l2 unified        512 KiB, 8-way, per core\n  l3 unified        32 MiB, 16-way, reported by this kernel as one instance\n                    shared by all 32 logical cpus (this is a WSL2 guest; the\n                    two CCDs of the physical part are not visible from inside\n                    it, so treat the L3 figure as a guest-visible number)\n  how it is timed   the process is pinned to one logical cpu (`taskset -c 4`)\n                    with a 250 ms settle gap between runs; its SMT sibling is\n                    left idle; 15 interleaved repetitions, shuffled label\n                    order, paired bootstrap 95% CI\n  compiler          rustc 1.100.0-nightly (bba531001), LLVM 23.1.1\n  baseline flags    -Copt-level=3, fat LTO, 1 codegen unit, -Cdebuginfo=1,\n                    -Ctarget-cpu=native, PGO (-Cprofile-use, one shared\n                    profile), -Cllvm-args=-hints-allow-reordering=false\n                    (that last flag means a width hint does **not** authorise\n                    reassociating a floating-point reduction; a\n                    non-reassociable reduction is simply not widened)\n\n## Where the hints are applied\n\nFunction attributes are applied at PipelineStartEP, before any inlining.\nLoop metadata is applied at VectorizerStartEP, after inlining and loop\ncanonicalisation and immediately before LoopVectorize, so a loop hint acts on\nthe loop as it exists after the function attributes of this same round have\nalready taken effect.\n\n## The marked functions of this program\n\n  jaq_json::read::parse                                         profile share 29.29%, 42 loop site(s)\n  <hifijson::SliceLexer as hifijson::token::Lex>::seq           profile share 13.05%, 0 loop site(s)\n  <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold   profile share 9.07%, 4 loop site(s)\n  <hifijson::SliceLexer as hifijson::write::Write>::write_unti  profile share 8.75%, 6 loop site(s)\n  <jaq_core::compile::TermId>::run                              profile share 8.32%, 14 loop site(s)\n  jaq_json::write::write                                        profile share 4.48%, 10 loop site(s)\n  <jaq_json::funs::base<jaq_all::data::DataKind>::{closure#3}   profile share 2.71%, 6 loop site(s)\n  <<jaq_core::path::Path<jaq_json::Val>>::run::{closure#0} as   profile share 2.53%, 8 loop site(s)\n  <core::io::write::default_write_fmt::Adapter<alloc::io::buff  profile share 2.45%, 0 loop site(s)\n  <hashbrown::raw::RawTable<usize>>::reserve_rehash::<indexmap  profile share 2.43%, 7 loop site(s)\n  <alloc::rc::Rc<indexmap::map::IndexMap<jaq_json::Val, jaq_js  profile share 2.37%, 2 loop site(s)\n  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as   profile share 1.73%, 1 loop site(s)\n  jaq_core::path::run                                           profile share 1.31%, 7 loop site(s)\n  <jaq_json::Val as core::hash::Hash>::hash                     profile share 1.17%, 12 loop site(s)\n  <&alloc::string::String as core::fmt::Display>::fmt           profile share 1.10%, 8 loop site(s)\n\n## Results of the previous rounds\n\n  | round | correct | aggregate speed ratio | 95% CI | accepted |\n  |---|---|---|---|---|\n  | 1 | yes | 0.9934 | [0.9862, 1.0006] | no |\n  | 2 | yes | 0.9976 | [0.9915, 1.0039] | no |\n\n  A round is accepted as the new best only if the lower end of its 95% CI is above the best point estimate so far.\n\n## The sites in this request (5 of them)\n\nEach section below is named after its question.\n\n### q0\n\nsite kind      one loop inside a marked function\nmarked function <&alloc::string::String as core::fmt::Display>::fmt\nowner after inlining  <&alloc::string::String as core::fmt::Display>::fmt\nlocation       macros.rs:180 (loop nesting depth 2)\nprofile share of the marked function  1.10%\naverage trip count (from the PGO profile)  32\nloop body      36 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:180: advising against unrolling the loop because it contains a invoke\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with complex writes to memory\n  macros.rs:180: loop not vectorized\n  macros.rs:180: loop not vectorized: Incorrect number of successors from early exiting block\n  macros.rs:180: advising against unrolling the loop because it contains a call\n  macros.rs:180: loop not vectorized: Cannot vectorize early exit loop with strided fault-only-first load\n  macros.rs:180: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  macros.rs:180: the cost-model indicates that vectorization is not beneficial\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <&alloc::string::String as core::fmt::Display>::fmt |\n  | that function's perf share (all six workloads) | 1.10% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 5.6% |\n  | average trip count (PGO) | 32.0 |\n  | loop header execution count (PGO) | 722,491,314 |\n  | body size | 36 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 2 |\n  | loops this site key names | 1 (the key names exactly one loop) |\n  | hotness (header count x body instructions) | 2.60e10 --- the largest hotness of any loop_in_mark key in this program |\n  | inline chain from the marked function down to the loop | Formatter::pad -> Chars::count -> do_count_chars -> <slice::Iter<[u8;4]>>::next |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit with complex writes to memory; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) + unroller advises against (contains a call/invoke) + cost model says not beneficial |\n\n### q1\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       mod.rs:1653 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  26\nloop body      141 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/write.rs:302 (lines 262-328, the site's own line marked `>`)\n  262  /// Write a value as JSON, using a custom function for child values.\n  263  #[macro_export]\n  264  macro_rules! write_val {\n  265      ($w:ident, $pp:ident, $level:expr, $v:ident, $f:expr) => {{\n  266          use $crate::Val::TStr;\n  267          match $v {\n  268              TStr(s) => style!($w, $pp, str, write_utf8!($w, s, |part| $w.write_all(part))),\n  269              _ => format_val!($w, $pp, $level, $v, $f),\n  270          }\n  271      }};\n  272  }\n  273  \n  274  /// Write a value as JSON.\n  275  ///\n  276  /// Note that unlike jq, this may actually produce invalid JSON.\n  277  /// In particular, this may yield:\n  278  ///\n  279  /// - literals for special floating-point values (NaN, Infinity, -Infinity)\n  280  /// - invalid UTF-8 characters\n  281  /// - byte strings with `\\xXX` sequences\n  282  /// - objects with non-string keys\n  283  ///\n  284  /// The key principles behind this behaviour are:\n  285  ///\n  286  /// 1. Printing a value should always succeed.\n  287  ///    (Otherwise, there would exist values that we could not even inspect.)\n  288  /// 2. Printing a value should yield valid JSON if and only if\n  289  ///    the value can be represented by an equivalent JSON value.\n  290  ///    (To give users a chance to find non-JSON values and to take appropriate action.)\n  291  ///\n  292  /// jq and jaq agree on principle 1, but disagree on principle 2.\n  293  /// In particular, this shows by the fact that `jq -n 'nan'` yields `null`.\n  294  /// That means that jq maps values that cannot be represented by JSON\n  295  /// to different values that can be represented by JSON.\n  296  ///\n  297  /// In summary,\n  298  /// jq may cause silent information loss, whereas\n  299  /// jaq may yield invalid JSON values.\n  300  /// Choose your poison.\n  301  #[cfg(feature = \"std\")]\n  302 >pub fn write(w: &mut dyn io::Write, pp: &Pp, level: usize, v: &Val) -> io::Result<()> {\n  303      write_val!(w, pp, level, v, write)\n  304  }\n  305  \n  306  pub(crate) struct Buf(pub(crate) alloc::vec::Vec<u8>);\n  307  \n  308  impl Buf {\n  309      fn write_all(&mut self, bytes: &[u8]) -> fmt::Result {\n  310          self.0.extend_from_slice(bytes);\n  311          Ok(())\n  312      }\n  313  }\n  314  \n  315  impl fmt::Write for Buf {\n  316      fn write_str(&mut self, s: &str) -> fmt::Result {\n  317          self.write_all(s.as_bytes())\n  318      }\n  319  }\n  320  \n  321  pub(crate) fn write_buf(w: &mut Buf, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  322      use core::fmt::Write;\n  323      write_val!(w, pp, level, v, write_buf)\n  324  }\n  325  \n  326  pub(crate) fn format(w: &mut Formatter, pp: &Pp, level: usize, v: &Val) -> fmt::Result {\n  327      format_val!(w, pp, level, v, format)\n  328  }\n\nwhat LLVM said about this region in the baseline build:\n  mod.rs:1653: advising against unrolling the loop because it contains a invoke\n  mod.rs:1653: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  mod.rs:1653: loop not vectorized: loop induction variable could not be identified\n  mod.rs:1653: loop not vectorized: Cannot vectorize early exit loop\n  mod.rs:1653: loop not vectorized: could not determine number of loop iterations\n  mod.rs:1653: loop not vectorized\n  mod.rs:1653: advising against unrolling the loop because it contains a call\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 25.8 |\n  | loop header execution count (PGO) | 144,524,134 |\n  | body size | 141 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 4 (one hint on this key lands on all four loops; their hotness sums to 3.24e10) |\n  | hotness (header count x body instructions) | 2.04e10 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> BufWriter<StdoutLock>::write_fmt -> default_write_fmt -> core::fmt::write |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (early exit; loop induction variable could not be identified; could not determine number of loop iterations) + unroller advises against (contains a call/invoke) |\n\n### q2\n\nsite kind      one loop inside a marked function\nmarked function <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nowner after inlining  <jaq_std::base_run<jaq_all::data::DataKind>::{closure#7} as core::ops::function::FnOnce<((jaq_core::filter::Ctx<jaq_all::data::DataKind>, jaq_json::Val),)>>::call_once\nlocation       macros.rs:279 (loop nesting depth 1)\nprofile share of the marked function  1.73%\naverage trip count (from the PGO profile)  222\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  macros.rs:279: the cost-model indicates that vectorization is not beneficial\n  macros.rs:279: the cost-model indicates that interleaving is not beneficial\n  macros.rs:279: loop not vectorized: runtime pointer checks needed. Enable vectorization of this loop with '#pragma clang loop vectorize(enable)' when compiling with -Os/-Oz\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 2)\n  macros.rs:279: completely unrolled vectorized loop with 3 iterations\n  macros.rs:279: vectorized loop (vectorization width: 4, interleaved count: 4)\n  macros.rs:279: completely unrolled loop with 7 iterations\n  macros.rs:279: completely unrolled loop with 2 iterations\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | <jaq_std::base_run<..>::{closure#7} as FnOnce<..>>::call_once |\n  | that function's perf share (all six workloads) | 1.73% |\n  | its share of the objsearch case | 0.0% |\n  | its share of the strproc case | 4.1% |\n  | its share of the readwrite case | 0.0% |\n  | average trip count (PGO) | 221.7 |\n  | loop header execution count (PGO) | 177,080,637 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.30e9 |\n  | inline chain from the marked function down to the loop | base_run{closure#7} -> map_utf8_str -> ByteSlice::to_ascii_lowercase -> <slice::Iter<u8>>::next |\n  | what the baseline remarks on this loop's line say | LEGAL and already vectorized: `vectorized loop (vectorization width: 4, interleaved count: 2)` and `(width 4, interleaved count: 4)` at this line, plus `completely unrolled`; cost model reports vectorisation and interleaving as not beneficial, and one `runtime pointer checks needed`. Remarks are attributed by source line only, and several loops share macros.rs:279 |\n\n### q3\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::read::parse\nowner after inlining  jaq_json::read::parse::<hifijson::SliceLexer>\nlocation       str.rs:194 (loop nesting depth 1)\nprofile share of the marked function  29.29%\naverage trip count (from the PGO profile)  14\nloop body      13 LLVM instructions, calls inside: no, floating-point reduction: no\nalready vectorized when the hint is attached: no\nother marked functions on this loop's inline chain: <hifijson::SliceLexer as hifijson::write::Write>::write_until, <hifijson::SliceLexer as hifijson::str::LexWrite>::str_fold\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\ntargets/jaq/src/jaq-json/src/read.rs:119 (lines 79-150, the site's own line marked `>`)\n   79  fn parse_string<L: LexAlloc>(lexer: &mut L, bytes: bool) -> Result<Vec<u8>, hifijson::Error> {\n   80      let on_string = |bytes: &mut L::Bytes, out: &mut Vec<u8>| {\n   81          out.extend(bytes.as_ref());\n   82          Ok(())\n   83      };\n   84      let s = lexer.str_fold(Vec::new(), on_string, |lexer, out| {\n   85          use hifijson::escape::Error;\n   86          match lexer.take_next().ok_or(Error::Eof)? {\n   87              b'u' if bytes => Err(Error::InvalidKind(b'u'))?,\n   88              b'x' if bytes => out.push(lexer.hex()?),\n   89              c => out.extend(lexer.escape(c)?.encode_utf8(&mut [0; 4]).as_bytes()),\n   90          }\n   91          Ok(())\n   92      });\n   93      s.map_err(hifijson::Error::Str)\n   94  }\n   95  \n   96  fn parse_num<L: LexAlloc>(lexer: &mut L) -> Result<Num, hifijson::Error> {\n   97      let num = hifijson::num::Num::signed_digits();\n   98      let (num, parts) = lexer.num_string_with(num).unvalidated();\n   99      let num = num.as_ref();\n  100      Ok(match num {\n  101          \"+\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::INFINITY),\n  102          \"-\" if lexer.strip_prefix(b\"Infinity\") => Num::Float(f64::NEG_INFINITY),\n  103          _ if num.ends_with(|c: char| c.is_ascii_digit()) => {\n  104              if parts.is_int() {\n  105                  Num::from_str_radix(num, 10).unwrap()\n  106              } else {\n  107                  Num::Dec(num.to_string().into())\n  108              }\n  109          }\n  110          _ => Err(hifijson::num::Error::ExpectedDigit)?,\n  111      })\n  112  }\n  113  \n  114  /// Parse a JSON value, given an initial non-whitespace character and a lexer.\n  115  ///\n  116  /// If the underlying lexer reads input fallibly (for example [`hifijson::IterLexer`]),\n  117  /// the error returned by this function might be misleading.\n  118  /// In that case, always check whether the lexer contains an error.\n  119 >fn parse<L: LexAlloc>(next: u8, lexer: &mut L) -> Result<Val, hifijson::Error> {\n  120      Ok(match next {\n  121          b'n' if lexer.strip_prefix(b\"null\") => Val::Null,\n  122          b't' if lexer.strip_prefix(b\"true\") => Val::Bool(true),\n  123          b'f' if lexer.strip_prefix(b\"false\") => Val::Bool(false),\n  124          b'b' if lexer.strip_prefix(b\"b\\\"\") => Val::byte_str(parse_string(lexer, true)?),\n  125          b'N' if lexer.strip_prefix(b\"NaN\") => Val::Num(Num::Float(f64::NAN)),\n  126          b'I' if lexer.strip_prefix(b\"Infinity\") => Val::Num(Num::Float(f64::INFINITY)),\n  127          b'0'..=b'9' | b'+' | b'-' => Val::Num(parse_num(lexer)?),\n  128          b'\"' => Val::utf8_str(parse_string(lexer.discarded(), false)?),\n  129          b'[' => Val::Arr({\n  130              let mut arr = Vec::new();\n  131              lexer.discarded().seq(b']', ws_tk, |next, lexer| {\n  132                  arr.push(parse(next, lexer)?);\n  133                  Ok::<_, hifijson::Error>(())\n  134              })?;\n  135              arr.into()\n  136          }),\n  137          b'{' => Val::obj({\n  138              let mut obj = Map::default();\n  139              lexer.discarded().seq(b'}', ws_tk, |next, lexer| {\n  140                  let key = parse(next, lexer)?;\n  141                  lexer.expect(ws_tk, b':').ok_or(Expect::Colon)?;\n  142                  let value = parse(ws_tk(lexer).ok_or(Expect::Value)?, lexer)?;\n  143                  obj.insert(key, value);\n  144                  Ok::<_, hifijson::Error>(())\n  145              })?;\n  146              obj\n  147          }),\n  148          _ => Err(Expect::Value)?,\n  149      })\n  150  }\n\nsource at the loop's innermost location (this is where the loop ended up after inlining, often inside the iterator machinery rather than in the marked function):\n/home/hiro/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/hifijson-0.5.0/src/str.rs:194 (lines 174-214, the site's own line marked `>`)\n  174  \n  175  impl<T> Lex for T where T: escape::Lex {}\n  176  \n  177  /// String lexing that allocates only when lexing from iterators.\n  178  pub trait LexWrite: escape::Lex + Read + Write {\n  179      /// Read a string to bytes, copying escape sequences one-to-one.\n  180      fn str_bytes(&mut self, bytes: &mut Self::Bytes) -> Result<(), Error> {\n  181          let mut state = State::default();\n  182          self.write_until(bytes, |c| state.process(c));\n  183          state.finish(|| self.take_next())\n  184      }\n  185  \n  186      /// Lex a string by executing `on_string` on every string and `on_bytes` on every escape sequence.\n  187      fn str_fold<E: From<Error>, T>(\n  188          &mut self,\n  189          mut out: T,\n  190          on_string: impl Fn(&mut Self::Bytes, &mut T) -> Result<(), E>,\n  191          on_escape: impl Fn(&mut Self, &mut T) -> Result<(), E>,\n  192      ) -> Result<T, E> {\n  193          fn string_end(c: u8) -> bool {\n  194 >            matches!(c, b'\\\\' | b'\"' | 0..=0x1F)\n  195          }\n  196  \n  197          let mut bytes = Self::Bytes::default();\n  198          self.write_until(&mut bytes, string_end);\n  199          on_string(&mut bytes, &mut out)?;\n  200          match self.take_next().ok_or(Error::Eof)? {\n  201              b'\\\\' => (),\n  202              b'\"' => return Ok(out),\n  203              0..=0x1F => return Err(Error::Control)?,\n  204              _ => unreachable!(),\n  205          }\n  206          loop {\n  207              on_escape(self, &mut out)?;\n  208              self.write_until(&mut bytes, string_end);\n  209              on_string(&mut bytes, &mut out)?;\n  210              match self.take_next().ok_or(Error::Eof)? {\n  211                  b'\\\\' => continue,\n  212                  b'\"' => return Ok(out),\n  213                  0..=0x1F => return Err(Error::Control)?,\n  214                  _ => unreachable!(),\n\nwhat LLVM said about this region in the baseline build:\n  str.rs:194: loop not vectorized: Loop contains an unsupported switch\n  str.rs:194: loop not vectorized: value that could not be identified as reduction is used outside the loop\n  str.rs:194: loop not vectorized: Incorrect number of successors from early exiting block\n  str.rs:194: loop not vectorized\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECsayvNaZMR3fG_3jaq' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECslKRKzoMO8pS_8jaq_json' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerEB4_' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXNtCs6u2SwoadH4g_8hifijson5writeNtB5_10SliceLexerNtB3_5Write11write_untilNvNvNtNtB5_3str8LexWrite8str_fold10string_endECstofKklK7j5_7jaq_all' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringNtCs6u2SwoadH4g_8hifijson10SliceLexerECstofKklK7j5_7jaq_all' because too costly to inline (cost=120, threshold=45)\n  str.rs:198: '_RINvXs_NtCs6u2SwoadH4g_8hifijson5writeINtB7_9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEENtB5_5Write11write_untilNvNvNtNtB7_3str8LexWrite8str_fold10string_endECsayvNaZMR3fG_3jaq' not inlined into '_RINvNtCslKRKzoMO8pS_8jaq_json4read12parse_stringINtCs6u2SwoadH4g_8hifijson9IterLexerNtNtNtCs2ubz5g4KsBC_4core2io5error5ErrorINtNtNtCsecSPvMcbw1R_5alloc2io4util5BytesNtNtNtCs1mKxMHUCrv1_3std2io5stdio9StdinLockEEECsayvNaZMR3fG_3jaq' because too costly to inline (cost=550, threshold=45)\n\n  what earlier rounds chose here:\n    round 1: KEEP_DEFAULT -> whole-build ratio 0.9934\n    round 2: KEEP_DEFAULT -> whole-build ratio 0.9976\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::read::parse |\n  | that function's perf share (all six workloads) | 29.29% |\n  | its share of the objsearch case | 33.9% |\n  | its share of the strproc case | 26.0% |\n  | its share of the readwrite case | 27.5% |\n  | average trip count (PGO) | 13.9 |\n  | loop header execution count (PGO) | 207,748,447 |\n  | body size | 13 LLVM instructions |\n  | calls inside the body | no |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 2.89e9 |\n  | inline chain from the marked function down to the loop | read::parse -> parse_string -> str_fold -> write_until -> Copied<Iter<u8>>::position -> position::check |\n  | what the baseline remarks on this loop's line say | ILLEGAL to vectorize (loop contains an unsupported switch; incorrect number of successors from early exiting block; value that could not be identified as a reduction is used outside the loop) |\n\n### q4\n\nsite kind      one loop inside a marked function\nmarked function jaq_json::write::write\nowner after inlining  jaq_json::write::write\nlocation       iter.rs:605 (loop nesting depth 1)\nprofile share of the marked function  4.48%\naverage trip count (from the PGO profile)  1\nloop body      200 LLVM instructions, calls inside: yes, floating-point reduction: no\nalready vectorized when the hint is attached: no\nfunction attributes this round already applied: none\n\nsource of the marked function this loop belongs to:\n  (not available: the source file is not in the vendored tree)\n\nwhat LLVM said about this region in the baseline build:\n  (not recorded: this site is outside the frozen site set of decision 63, so\n   no remark attribution was run for it)\n\n  what earlier rounds chose here:\n    (this site was not asked about in an earlier round)\n\nprofile and shape of this loop, as a table\n\n  | field | value |\n  |---|---|\n  | marked function it belongs to | jaq_json::write::write |\n  | that function's perf share (all six workloads) | 4.48% |\n  | its share of the objsearch case | 0.1% |\n  | its share of the strproc case | 0.0% |\n  | its share of the readwrite case | 22.6% |\n  | average trip count (PGO) | 1.0 |\n  | loop header execution count (PGO) | 5,591,880 |\n  | body size | 200 LLVM instructions |\n  | calls inside the body | yes |\n  | loop nesting depth | 1 |\n  | loops this site key names | 1 |\n  | hotness (header count x body instructions) | 1.12e9 |\n  | inline chain from the marked function down to the loop | jaq_json::write::write -> <slice::Iter<..>>::next |\n  | what the baseline remarks on this loop's line say | not recorded for this site (it is outside the frozen site set, so no remark attribution was run for it) |\n"}, "sha": "1b78ba15a58e0a2e", "site_map": {"q0": "L1", "q1": "L2", "q2": "L3", "q3": "L4", "q4": "L5"}, "stage": null, "tag": "V16-L1-L2-L3-L4-L5", "variant": "V16"}
